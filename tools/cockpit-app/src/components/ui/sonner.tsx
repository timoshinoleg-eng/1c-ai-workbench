// Sonner toast wrapper. The actual <Toaster /> is mounted in main.tsx; this
// file re-exports the `toast` function so call sites can import from a
// consistent shadcn-style path: `@/components/ui/sonner`.
export { toast } from "sonner";
