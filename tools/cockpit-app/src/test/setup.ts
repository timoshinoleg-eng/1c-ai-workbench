// Minimal Vitest setup. Add global mocks here (matchMedia, ResizeObserver, etc.)
// once the test surface grows. Importing `@testing-library/jest-dom` would
// extend `expect`; intentionally omitted for the v0.1.0 scaffold.

import "@/i18n";
