# Changelog

Формат — [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).
Версионирование — [SemVer](https://semver.org/lang/ru/).

## [0.45.0] — 2026-07-10

**Парсер BSL переведён с `tree-sitter-onescript` на специализированную грамматику `tree-sitter-bsl` (0.1.7) со слоем нормализации `bsl-parse`. Грамматика OneScript была приближением встроенного языка 1С; `tree-sitter-bsl` разбирает BSL точнее, а `bsl-parse` обходит известные дефекты грамматики (нормализация исходника перед разбором). Ключевой эффект — конструкторы платформенных типов (`Новый Массив`, `Новый ТаблицаЗначений`, `Новый Структура`) больше не попадают в граф вызовов как вызовы процедур. Требуется полная переиндексация.**

### Изменено

- **Грамматика BSL: `tree-sitter-onescript` → `tree-sitter-bsl` 0.1.7.** Удалена зависимость `tree-sitter-onescript` из workspace, `code-index-core` и `bsl-extension`. Файлы `.bsl` и `.os` разбираются грамматикой `tree-sitter-bsl`.
- **Единый слой разбора `bsl-parse` (крейт `crates/bsl-parse`).** Нормализация исходника под дефекты грамматики `tree-sitter-bsl` (`normalize_for_parser`) перед подачей в парсер. Крейт вендорен в репозиторий (копия единого слоя, общего с проектом bsl-context) — code-index собирается самодостаточно, без внешних path-зависимостей.
- **Граф вызовов: убраны ложные рёбра от конструкторов.** `Новый Массив`/`ТаблицаЗначений`/`Структура`/`Соответствие` и прочие конструкторы платформенных типов больше не резолвятся как вызовы одноимённых процедур. На реальной УТ 11.5: `Массив` 3525 → 0 рёбер, `total_calls` 2 139 478 → 2 021 203 (−118 тыс. ложных). Симметрично на ЗУП, БП Смак-султана, БП ТДК.

### Тестирование

- `cargo test --workspace --features enrichment` — 581 passed, 0 failed, 0 warnings.
- Локальный live-smoke на выгрузке WMS (59 модулей): индексация, граф вызовов (`get-callees` резолвит квалифицированные вызовы), FTS, `grep-body` — зелёные.
- Федеративная раскатка на ВМ (musl-ELF): переиндексация всех четырёх боевых баз — `ut` (58 тыс. файлов), `zup` (40 тыс.), `bp-ss` (94 тыс.), `bp-tdk` (90 тыс.), `exit=0` на каждой. Эффект подтверждён на всех: `Массив` = 0, остались только настоящие вызовы `ТаблицаЗначений` (4–5 рёбер).

### Совместимость

- **Требуется полная переиндексация** (`index --force`): граф вызовов пересобирается новым парсером. Схема БД не изменена.

## [0.44.4] — 2026-07-08

**Инкрементальная пересборка объекта при уходе заимствователя приведена к полной симметрии с полным reindex: `data_links`, `attributes_json` и `metadata_modules` восстанавливаются слиянием по ВСЕМ оставшимся копиям (база + расширения), а не по одному пришедшему файлу. На реальной УТ 11.5 инкремент после ухода заимствователя совпал с полным пересбором по всем 9 extras-таблицам (было расхождение `data_links` и `config_version` форм-модулей).**

**Отдельно устранена квадратичная деградация инкрементальной пересборки графа вызовов на bulk-батче: резолв адресов вызываемых процедур (`callee_proc_key`) вынесен из пофайлового цикла в один-на-батч (общий хелпер с полным пересбором). На реальной УТ 11.5 (git-накат 2921 файла, Update_20260623 -> Update_20260707) батч из 3193 файловых событий: 585 сек -> 25,6 сек (~23x быстрее), прежнее зависание >12 мин устранено.**

> **Область фикса — объекты ВНУТРИ расширений (уход заимствования конкретного объекта: расширение перестаёт заимствовать объект — его копия удалена, опись обновлена). Удаление расширения ЦЕЛИКОМ инкрементом НЕ обрабатывается** (нет надёжного сигнала: опись расширения удаляется, а не модифицируется, watcher-событие удаления ненадёжно) — этот случай штатно закрывается полной переиндексацией при рестарте демона.

> Когда заимствователь уходит, `remerge_object` пере-собирал объект по base+оставшимся копиям для `metadata_objects.attributes_json`, но `data_links` разбирал по одному пришедшему файлу (фантомные рёбра расширения или потеря базовых), а `metadata_modules` не трогал вовсе → `config_version` форм-модулей заимствователя устаревал (модуль-сирота не пересобирался). Полный reindex давал иной результат.

### Исправлено

- **`data_links` при уходе заимствователя — слияние по копиям.** `update_data_links_for_object` (в `remerge_object`) удаляет все рёбра объекта и заново собирает их по всем существующим копиям во всех sub-config (симметрично bulk `index_data_links`); ушедшая копия отсеивается сама (файла нет). Закрывает: A — delete копии доставлен watcher-ом (пофайловый разбор сносил ВСЕ рёбра, включая базовые); A2 — delete потерян, только MODIFY описи (рёбра-фантомы расширения оставались); B — изменена копия расширения (базовое ребро терялось).
- **`metadata_modules` при уходе заимствователя — пересборка модулей объекта.** Новая `update_metadata_modules_for_object` (в `remerge_object`): DELETE всех модулей объекта (по `object_name`, все `extension_name`) + обход `.bsl`-модулей объекта во ВСЕХ roots с повторной вставкой по свежей описи. Без неё `config_version` форм-модулей заимствователя оставался устаревшим (пофайловый путь их не трогает — `.bsl` физически не менялся). Общая логика вынесена в `build_module_row`/`insert_module_row`, переиспользуется пофайловым `update_metadata_module_for_file`.
- **`sub_config_roots` и кэш описей (`cfgver_cache`) считаются один раз на пачку** — `parse_config_dump_info` не перечитывается для каждого объекта/модуля.
- **Квадратичная деградация инкремента графа вызовов на bulk-батче — резолв `callee_proc_key` вынесен в один-на-батч.** `update_call_graph_direct_for_file` больше не резолвит адреса целей пофайлово: каждый `.bsl` заново сканировал весь `proc_call_graph` через `substr(caller_proc_key…)` (без индекса) и пересобирал tmp-карту экспортов общих модулей полным обходом `files x functions` (~57K x 260K на КАЖДЫЙ файл). Теперь пофайлово — только сырые рёбра файла (по индексам, `callee_proc_key = NULL`), а глобальный резолв и прунинг вынесены в общий хелпер `resolve_and_prune_direct_edges` (тот же, что зовёт полный `build_call_graph`), выполняемый ОДИН раз после цикла батча в `run_incremental_extras`. Идентичность инкремент==полный по `proc_call_graph` сохранена (общий код + parity-тесты).
- **`procedure_enrichment` — полный скан заменён на поиск по индексу.** В `update_procedure_terms_for_file` условие `proc_key LIKE ?||'::%'` (SCAN ~257K строк, LIKE не использовал индекс) заменено на range `proc_key >= '<rel>::' AND proc_key < '<rel>:;'` (SEARCH по `idx_pe_proc_key`, подтверждён `EXPLAIN QUERY PLAN`).

### Тестирование

- Юнит-тесты симметрии инкремент==полный: `incremental_borrower_drop_keeps_data_links` (A), `..._opis_only_...` (A2), `incremental_ext_copy_change_keeps_base_data_links` (B), `incremental_massive_object_change_matches_full` (C, N=60), новый `incremental_borrower_drop_rebuilds_metadata_modules`. Для резолвера графа вызовов — новый многофайловый parity-тест `incremental_call_graph_multifile_batch_matches_full` (батч из 2 общих модулей с кросс-ссылками экспортов, инкремент==полный) плюс существующие `incremental_call_graph_direct_matches_full` / `incremental_direct_shared_edge_survives`. Весь workspace зелёный (538 тестов: 192 core + 23 indexer + 323 bsl-extension, 0 failed, 0 warnings).
- **Федеративный «злой» замер на реальной УТ 11.5** (57 тыс. файлов, 43 sub-config): уход заимствователя `Document.ЗаказКлиента` из расширения `ent_УправлениеДоставками` → инкремент **совпал с полным reindex по всем 9 extras-таблицам** (`data_links`, `metadata_objects`, `config_manifest`, `metadata_modules`, `metadata_forms`, `role_rights`, `event_subscriptions`, `metadata_code_usages`, `proc_call_graph`). До фикса `metadata_modules` расходилась по `config_version` форм-модулей.
- **Массовый нагрузочный замер:** снятие заимствования расширения `МодульУОП` целиком (1046 файловых событий, пере-собрано 1036 объектов) — инкремент **5,4 сек против ~60 сек полной индексации** (×11 быстрее, деградации от пересборки нет).
- **Массовый git-based замер резолвера на реальной УТ 11.5** (клон боевого gitlab, накат Update_20260623 -> Update_20260707, diff 2921 файла): инкрементальный батч из 3193 файловых событий (changed 1746 + deleted 1447) обработан за **25,6 сек** против **585 сек** на прежнем пофайловом резолвере (и прежнего зависания >12 мин на повторном прогоне). Полная индексация клона для сравнения — 163 сек (core 71 + extras 92). Второй батч (426 файлов, 39,7 сек) — стоимость config-level пересбора (`data_links` config, `role_rights`, reconcile базы), а не графа вызовов.

### Совместимость

- Формат данных и схема БД не изменены — переиндексация не требуется. Правка затрагивает только инкрементальный путь reconcile-области при уходе заимствователя; первичная индексация, bulk-обновление и пофайловый путь не тронуты.

## [0.44.3] — 2026-07-05

**Устранена квадратичная деградация bulk-обновления существующей БД: старые строки изменённых файлов удаляются одним пакетным проходом, пока индексы живы, вместо построчного DELETE без индексов. На УТ-масштабе (2 млн calls) фаза удаления при обновлении 500 файлов — 260 385 мс → 234 мс (×1113); реиндекс после массового git pull: часы → минуты.**

> При bulk-обновлении непустой БД (число изменённых файлов > `bulk_threshold`) `prepare_bulk_load()` дропал вторичные индексы `idx_*_file` ДО цикла записи, а цикл на каждый файл выполнял по 5 `DELETE … WHERE file_id = ?` уже без индексов → полный скан таблиц на каждый файл. На УТ (`calls` ~2 млн строк × тысячи изменённых файлов) это давало десятки млрд просмотров строк на одном ядре — часы вместо минут. Первичная индексация (пустая БД) и инкрементальный путь (< порога, индексы живы) не затрагивались и не менялись.

### Исправлено

- **Пакетное удаление старых строк одним проходом, пока индексы `idx_*_file` живы.** В ветке bulk-обновления непустой БД теперь: (1) снимаются FTS-триггеры functions/classes, чтобы пакетный DELETE не дёргал их построчно; (2) старые строки изменённых файлов (только тех, что уже есть в БД) удаляются `DELETE … WHERE file_id IN (…)` чанками по 5 «кодовым» таблицам (`functions`, `classes`, `imports`, `calls`, `variables`) одной транзакцией — пока вторичные индексы ещё на месте; (3) только потом дропаются B-tree индексы. В цикле записи `skip_delete = bulk_mode || is_fresh_db` — построчные DELETE больше не выполняются, старые строки уже удалены. `finish_bulk_load()` (пересоздание индексов, триггеров, FTS rebuild) без изменений.
- **Текст/FTS не трогается пакетно намеренно.** `text_contents` имеет `file_id` первичным ключом (удаление дёшево), а contentless-указатель `fts_text_files` идемпотентно пересобирается в `insert_text_file` при повторной записи файла — отдельный пакетный проход не нужен и был бы неверным (нужен старый текст).
- `drop_indexes_and_triggers()` разбит на `drop_indexes()` + `drop_fts_triggers()`; прежняя функция сохранена как их композиция.

### Тестирование

- Новый юнит-тест `test_bulk_update_existing_db`: после bulk-обновления счётчики `functions`/`classes`/`imports`/`calls`/`variables`/`text` строго равны свежей индексации того же финального среза (нет дублей), FTS находит новые символы и не находит старые. Весь `code-index-core` зелёный (322 теста, 0 failed), workspace зелёный.
- Live-smoke НА ДИСКЕ на реальной конфигурации УТ 11.5 (копия, 57 тыс. файлов, БД 4,3 ГБ, `calls` 2 075 353). A/B старый бинарник (0.44.2) vs новый на bulk-обновлении 500 изменённых файлов: фаза «Запись в БД» **260 385 мс → 234 мс (×1113)**; все 7 таблиц счётчиков идентичны (старый корректен, но медленный). Экстраполяция на git-pull ~15 тыс. файлов: фаза удаления часы → секунды.

### Совместимость

- Формат данных и схема БД не изменены — переиндексация не требуется, существующие индексы полностью совместимы. Правка затрагивает только путь bulk-обновления непустой БД; первичная индексация и инкрементальный путь не тронуты.

## [0.44.2] — 2026-07-04

**Обязательный параметр `repo` теперь единообразно описан в JSON-схеме ВСЕХ инструментов с явной пометкой «ОБЯЗАТЕЛЕН», а у `read_file`/`stat_file` обязательность вынесена и в описание инструмента. Слабые модели переставали угадывать этот параметр и роняли вызов (GitHub issue #3).**

> У `read_file`/`stat_file`, `list_files`, `grep_text`, `grep_code` поле `repo` вообще не имело описания — в `tools/list` модель не видела ни назначения параметра, ни того, что он обязателен. У остальных инструментов описание было, но без указания на обязательность. Слабые локальные модели (например, qwen3-4b через LM Studio) вызывали `read_file` только с `path` и получали невнятную ошибку. Правка только в описаниях схемы, поведение и данные не менялись.

### Исправлено

- **Единообразное описание поля `repo` во всех структурах параметров.** У каждого `repo: String` теперь doc-комментарий «ОБЯЗАТЕЛЕН. Алиас репозитория (из --path alias=dir…). Без него вызов не пройдёт. Список — get_stats.» — раньше у части инструментов (`read_file`, `stat_file`, `list_files`, `grep_text`, `grep_code`) описание отсутствовало, у остальных не подчёркивало обязательность. Опциональный `repo` у `get_stats` (`Option<String>`) не тронут. `repo` уже был обязателен на уровне типов; правка делает это видимым для модели в `tools/list`.
- **`read_file` / `stat_file`: обязательность обоих параметров вынесена в описание инструмента.** В `#[tool(description=…)]` добавлено, что обязательны `repo` и `path`; поле `path` также получило краткое описание.

### Тестирование

- Весь `code-index-core` зелёный (321 тест, 0 failed).
- Live-smoke собранного `code-index serve` (HTTP, MCP-хендшейк → `tools/list`): у `read_file`, `stat_file`, `list_files`, `grep_code`, `grep_text`, `search_function`, `get_function`, `find_symbol` в `inputSchema.properties.repo.description` непустой текст с «ОБЯЗАТЕЛЕН», `repo` присутствует в `required`; у `get_stats` `repo` остаётся опциональным (`required` пуст).

## [0.44.1] — 2026-07-03

**Ускорение фазы extras полной индексации: сбор термов процедур в параллельном парсинге, пакетная пересборка полнотекста, разовая материализация графа вызовов. Требуется переиндексация для эффекта; индекс побайтно идентичен прежнему.**

> Профилирование полной индексации BP_TDK (88279 файлов) показало, что в extras доминируют не поиск, а фазы записи: построчная работа с индексами/триггерами и повторное чтение того, что уже распарсено ядром. Три правки убирают лишнюю работу, не меняя данные (проверено sha256 графа и термов против прежней БД). Затрагивают только путь ПОЛНОЙ переиндексации; инкрементальный путь и публичный `code-index` не тронуты.

### Изменено

- **`procedure_terms`: сырьё собирается в параллельном парсинге ядра (54 с → 17 с на BP_TDK).** Раньше слой термов повторно читал все `.bsl` с диска и построчно наполнял `procedure_enrichment`, у которой на поле `terms` висит FTS5 с триграммным токенайзером — токенизация ~530 тыс. строк построчными триггерами была доминантой стоимости. Теперь имя/комментарий/объект собираются прямо из горячих `parse_results` во время параллельного парсинга (как `code_usages` в 0.x), синоним подставляется JOIN'ом уже после XML-слоя синонимов, а полнотекст строится одним `INSERT … VALUES('rebuild')` со снятием FTS-триггеров на время массовой вставки и гарантированным возвратом их обратно. Термы ~530 тыс. процедур BP_TDK побайтно те же (sha256 совпал).
- **Граф вызовов: `calls⋈files` материализуется один раз (93 с → 52 с на BP_TDK).** Построение `proc_call_graph` (direct-рёбра) и `direct_edge_files` делало дорогой `JOIN … DISTINCT` по `calls⋈files` дважды — по разу на таблицу — и в паре с построчной вставкой в индексируемую таблицу деградировало сильнее суммы частей. Теперь множество рёбер `(path, caller, callee)` собирается `DISTINCT`'ом во временную таблицу один раз, из неё обе таблицы наполняются простыми вставками без повторного JOIN/DISTINCT. Граф побайтно идентичен (sha256 всех рёбер совпал).

### Тестирование

- Полный `index --force` на BP_TDK: `procedure_enrichment`(mech) = 529756, `metadata_code_usages` = 260166, `proc_call_graph` = 1236771 (direct 1165856), `direct_edge_files` = 2424236 — все совпадают с прежней БД; sha256 графа и sha256 термов идентичны. FTS синхронен (fts = content = 529756), триграммный поиск работает, FTS-триггеры и индексы графа целы после переиндексации.
- Весь workspace зелёный (514 тестов, 0 failed), включая `procedure_enrichment_inserts_propagate_to_fts`, `procedure_enrichment_update_resyncs_fts`, `incremental_terms_update_and_cleanup`, `proc_call_graph_unique_constraint_works`.
- Диффом подтверждено: инкрементальные функции (`update_procedure_terms_for_file`, `update_code_usages_for_file`, `update_call_graph_direct_for_file`) не изменены — правки только в пути полной переиндексации.

### Совместимость

- **Требуется полная переиндексация** для получения ускорения (правки в пути full-reindex). Формат данных не изменён: старый индекс продолжает работать, ускорение появляется при следующей полной переиндексации (`index --force` или пересоздание `.code-index`).

## [0.44.0] — 2026-07-02

**Подсказки `did_you_mean` с похожими именами при пустом `get_function`/`get_class` + косметические уточнения подсказок (динамические вызовы в графе).**

> Продолжение работы по статистике вызовов **Юрия Гридунова** (см. 0.43.0): после устранения регистровых промахов остались повторные слепые вызовы с вариациями имени и ложный вывод «0 вызовов = мёртвый код» на динамических диспетчерах. Обе правки — слой выдачи serve, переиндексация не требуется.

### Добавлено

- **`did_you_mean` в `get_function`/`get_class`.** Если и точный, и регистронезависимый поиск вернули 0, в ответ добавляется до 5 похожих имён. Кандидаты собираются из двух взаимодополняющих источников: префикс-LIKE по имени с пробами от длинного префикса к короткому (12 → 9 → 6 символов — на «горячих» началах имён 1С вроде `Провер%`/`Заполн%` короткий префикс с лимитом набирает случайные имена и теряет цель) и FTS (совпадение по токенам-частям — имена с `_`). Ранжирование — расстояние Левенштейна без учёта регистра с порогом адекватности (треть длины запроса, минимум 3): лучше пустой `did_you_mean`, чем пять посторонних имён из общего префикса. Имя-«продолжение» (`ЗаполнитьЖурналОпераций` → `…ОперацийМаксимо`) не штрафуется за длинный хвост. Тот же паттерн, что уже был у `get_object_structure` и `bsl_sql`.

### Косметические изменения

- **Уточнена подсказка `get_callers`/`get_callees` при 0 рёбер.** Дописано явное предупреждение: «0 вызовов ≠ мёртвый код» — функция может вызываться динамически (`Выполнить`/`Вычислить` со сборкой имени из строк, типовой паттерн диспетчеров), рекомендация проверить `grep_code` по фрагменту имени (он видит и строковые литералы). Закрывает частую ошибку модели «нет рёбер → код мёртвый» текстом, без изменений индексатора: замер по 6 боевым репо показал ~42 реальных диспетчера имён на 5 конфигураций — отдельная таблица кандидатов динамических вызовов не окупается.

### Тестирование

- Юнит-тесты: подсказки на опечатке в хвосте слова, lowercase+опечатка, отсутствие мусорных подсказок на несуществующем имени, зеркальный тест для классов. Весь workspace зелёный (code-index-core 321, bsl-extension 168, integration 23, 0 failed).
- Live-smoke локально и через federation: `ОбработатьЗапрсо` → `ОбработатьЗапрос` (wms, единственная подсказка — шум срезан порогом), `ПроверитьУсловияТригера` → `ПроверитьУсловияТриггера` первым (ut, «горячий» префикс), новая подсказка графа вызовов с обеих нод, регрессия ci-fallback (`уоп_подключаемыекоманды_выполнить` → 31 локация).

## [0.43.0] — 2026-07-01

**Устойчивость навигации по коду 1С к регистру и к путанице параметров: регистронезависимый поиск символов, приём `name` в графе вызовов, уточнённые описания grep-инструментов.**

> Контекст. Спасибо **Юрию Гридунову** за подробную статистику обращений к code-index из реальной сессии массового документирования 1С-интеграции (Composer + Sonnet, ~260 вызовов инструментов) — она точно вскрыла повторяющиеся точки трения на кириллице и подсказала, что чинить. Все три правки — обратносовместимые, переиндексация не требуется (только слой выдачи serve).

### Добавлено

- **Регистронезависимый поиск символов в `get_function`/`get_class`.** Точное совпадение по имени в SQLite побайтовое, и регистр кириллицы им не сворачивается: `заполнитьжурнал` не находил `ЗаполнитьЖурнал`, хотя функция есть. Теперь при пустом точном совпадении включается fallback через FTS (токенайзер `unicode61` сворачивает регистр кириллицы) со строгой ci-сверкой имени в Rust — быстрый путь точного поиска не затронут, дорогой fallback исполняется только когда иначе вернулся бы 0. Ловит частый класс ошибок модели «имя из памяти в нижнем регистре»; корректно работает и на именах с подчёркиваниями (`уоп_подключаемыекоманды_выполнить`).

### Изменено

- **`get_callers`/`get_callees` принимают `name` (и `symbol`) как синоним `function_name`.** Модели регулярно путали ключ параметра с `get_function`, и слепой вызов падал тёмной ошибкой разборщика «missing field function_name» — потерянный ход. Добавлен serde-alias (как уже было у `find_symbol`). Канонический `function_name` работает как прежде.
- **Уточнены описания `grep_code` и `grep_body`.** Описание `grep_code` ошибочно подавало его как дополнение к `grep_body` («всё что не ловит grep_body»), хотя фактически он ищет по ПОЛНОМУ тексту файла (module-level + тела) — надмножество. Теперь описания явно разграничивают: `grep_code` — все вхождения строки/имени где угодно в файле (таблицы маршрутизации, объявления `Перем`, литералы + тела); `grep_body` — только тела, но с указанием, в какой именно процедуре найдено. Устраняет частый промах «взял `grep_body` для имени службы и пропустил его использование на уровне модуля».

### Тестирование

- Юнит-тесты: ci-fallback (кириллица + подчёркивания + отсутствие ложных срабатываний), fallback для классов, десериализация alias. Весь workspace зелёный (code-index-core 319, bsl-extension 168, 0 failed).
- Live-smoke на реальных данных: локальный serve (lowercase `get_function` → тело функции) и федеративный узел через federation (lowercase `уоп_подключаемыекоманды_выполнить` по `ut` → 31 локация).

## [0.42.2] — 2026-06-30

**Поддержка формата выгрузки 1С:EDT: разбор метаданных `.mdo` и защита парсера от двоичных модулей.**

> Контекст. Конфигурации, выгруженные через 1С:EDT (а не Конфигуратор), хранятся в ином формате: метаданные лежат в XML-файлах `.mdo` (схема `mdclass`), а защищённые модули поставщика EDT кладёт под расширением `.bsl` с двоичным образом вместо текста (Конфигуратор для тех же модулей использует `.bin`). Релиз добавляет разбор `.mdo` и закрывает деградацию парсера на таких двоичных «.bsl».

### Добавлено

- **Разбор метаданных выгрузки 1С:EDT (`.mdo`).** Новый слой строит те же таблицы (`metadata_objects`, `data_links`, `metadata_forms`, `event_subscriptions`), что и для конфигураторной выгрузки, — обходом `src/<Тип>/<Имя>/<Имя>.mdo` по всем типам объектов. Downstream-инструменты (`get_object_structure`, `get_object_profile`, `get_data_links`, `get_register_writers`, `get_form_handlers`, `get_event_subscriptions`, `find_references` и др.) работают на EDT-репозиториях без изменений. Распознавание формата — по `Configuration.mdo` в корне выгрузки.

### Исправлено

- **Зависание индексации на двоичных «.bsl» из EDT-выгрузки.** Защищённые модули поставщика EDT хранит как `ObjectModule.bsl` с двоичным контейнером 1С (сигнатура `FF FF FF 7F`) вместо исходного текста. tree-sitter на таком вводе деградировал квадратично — один модуль 1.3 МБ занимал десятки минут на одном ядре и подвешивал индексацию всего репозитория. Добавлен детект двоичного контента на сырых байтах (в `file_hash`): файл с сигнатурой контейнера или NUL-байтом помечается бинарным и не отдаётся в парсер. Как страховка от любой иной патологии — дедлайн на парсинг одного файла (10 с) в BSL-парсере. Проверено на ~81 000 `.bsl` (5 конфигураций, выгрузки Конфигуратора и EDT): ложных срабатываний нет.

### Тестирование

- 316 unit/integration-тестов зелёные (включая 2 новых на детект двоичного контента).
- Live-smoke на EDT-выгрузке БП ТДК (23 155 файлов): индексация 70 с против бесконечного зависания до фикса.
- Регресс на федеративном Конфигуратор-репозитории БП ТДК (90 024 файла, force-reindex с нуля): статистика индекса бит-в-бит совпала с прежней — защита на нормальных репозиториях строго no-op.

## [0.42.1] — 2026-06-29

**Косметический релиз: внутренние правки чистоты кода без изменения поведения. Выдача инструментов, индексация и протокол не затронуты. Переиндексация не требуется.**

> Контекст. Точечная уборка по итогам внешнего разбора кода: устранён clippy-паттерн `never_loop`, удалён дубль в слое выдачи grep и мёртвая функция в индексаторе. Наблюдаемое поведение идентично 0.42.0 (подтверждено: 314 тестов зелёные, сравнение выдачи grep до/после, live-smoke на обеих нодах — локальной и федеративной).

### Изменено

- **Дедуп grep-слоя: общий стриминговый помощник `grep_zstd_stream`.** `grep_code_filtered` и `grep_text_filtered` (storage) делили ~80 строк идентичного пост-процессинга (zstd-decode → построчный regex → `context_lines` → потолки `limit`/байт). Вынесено в один помощник, принимающий итератор строк, — заодно убрана промежуточная материализация всех blob'ов в `Vec` (ранний выход по лимитам не читает остаток). Контракт выдачи (`GrepTextMatch`, порядок, `truncated`) не изменён.
- **`extract_docstring` (Python-парсер): убран паттерн `clippy::never_loop`.** Цикл `for`, всегда делавший одну итерацию с безусловным `break`, заменён на явное чтение первого узла body (`next()?`). Логика извлечения docstring идентична.

### Удалено

- **Мёртвая функция `collect_candidates_standalone` (индексатор).** Полный дубль метода `collect_candidates` без единого вызова в кодовой базе. Живой путь сбора кандидатов (`Indexer::collect_candidates`, используемый `full_reindex`) не затронут.

## [0.42.0] — 2026-06-24

**Кэш результатов tool-вызовов + сессионный дедуп ре-доставки + ПО-ФАЙЛОВАЯ свежесть и инвалидация — всё ВНУТРИ serve. Срез служебного `_meta` перенесён в serve: отдельный прокси-кэш mcp-cache-ci перед serve в ci-цепочке больше не нужен. Переиндексация НЕ требуется (всё на serve-слое выдачи).**

> Контекст. Кэширование ответов жило в отдельном прокси mcp-cache-ci перед serve — лишний хоп, второй слой кэша, и сессионная идентичность (для дедупа) до serve доходила нечисто. Перенос внутрь serve делает его самодостаточным в ci-цепочке. Свежесть и инвалидация сделаны ПО ФАЙЛУ (паритет со снятым прокси): страдают только запросы, реально зависящие от изменённого файла, а не весь репо.

### Добавлено

- **Кэш результатов внутри serve (`ServeCache`, `serve_cache.rs`).** Кросс-сессионный (ключ `{repo}|{tool}|{sha256(args без repo)}`, БЕЗ session — результат переиспользуется между разговорами), хранит полный ответ, TTL 3600с (подстраховка к инвалидации). Кэшируются только LOCAL-репо; `health`, `get_stats` и federation-репо минуют кэш (federation кэшируется на удалённой ноде).
- **Сессионный дедуп ре-доставки строк (`SessionDedup`, `serve_dedup.rs`).** Ключ — `mcp-session-id` из HTTP-заголовка (rmcp вкладывает `http::request::Parts` в `context.extensions`). Опускает строки табличного результата (`result.rows`), уже отданные В ЭТОЙ сессии, со счётчиком-маркером `rows_elided_already_delivered:N`. Нетабличные ответы не трогает. Слой отдельный от кэша: кэш хранит полный результат (session-независимый), дедуп урезает уже на выдаче по сессии. Другая сессия с тем же запросом получает полный результат.
- **По-файловая свежесть + событийная инвалидация watcher→serve.** Демон шлёт `POST /mark-dirty {repo, files:[{path, mtime}]}` (файлы изменены на диске с observed-mtime, индекс ещё не догнал) и post-commit `POST /invalidate {repo, file_paths}`. Ответ НЕ кэшируется и НЕ отдаётся из кэша **только** если его файл-источник «грязный» — observed-mtime на диске новее `index_mtime` из `_meta.file_mtimes` ответа; инвалидация по файлу сносит лишь ключи, зависящие от него (обратный индекс «файл → ключи»). Запросы про не тронутые файлы не страдают — **без огрубления на весь репо**. Новые HTTP-роуты serve: `/mark-dirty`, `/invalidate`, `/cache-stats` (наблюдаемость). Канал подключается строкой `[[cache_targets]] url="http://127.0.0.1:8013"` в `daemon.toml` (демон шлёт один payload всем target'ам). `invalidate` с `all`/без `file_paths` — полный/репо-сброс (force-reindex).

### Изменено

- **Срез служебного `_meta` перенесён из прокси в serve (`finish` → `strip_meta`).** Горло выдачи `finish` (все пути возврата `call_tool`, включая federation-форвард) убирает `_meta` (`dependent_files`/`file_mtimes`) из ответа клиенту — три формы: `content[*].text` (вложенный JSON), top-level `{result,_meta}`, `structuredContent._meta`. К моменту выдачи `_meta` уже использован каналом свежести/инвалидации, клиенту (модели) не нужен и раздувает контекст. Зеркало `strip_meta` прокси mcp-cache-ci. Итог: serve самодостаточен в ci-цепочке — отдельный прокси-кэш для ci не обязателен, клиенты ходят на serve напрямую.
- **Кэш и дедуп на serve включены по умолчанию** (TTL 3600с — захардкожен; вынос в конфиг — следующий шаг). Не затронуто: индексация (демон сравнивает `content_hash`/`mtime` по SQLite, не по выдаче), граф связей/вызовов, BSL-инструменты, federation-протокол, **конкурентная обработка запросов** (многопоточный tokio + пул соединений SQLite — короткие локи кэша без удержания через `await`).

## [0.41.0] — 2026-06-23

**Критерий-селектор `name_like` (+ опц. `meta_type`) у `get_object_structure`: структуры ВСЕХ объектов одной темы за один вызов, без перечисления имён. Переиндексация НЕ требуется (serve-слой). Эмпирически модель берёт его охотно — это параметр на уже-используемом инструменте, в отличие от generic SQL.**

> Контекст. Модель опрашивает структуры объектов по одному (серия `get_object_structure`). Форма списка `full_names[]` спонтанно не приживается (требует сперва перечислить все имена). Плоский предикат по имени объекта эту серию схлопывает в один ход — и, в отличие от `bsl_sql`, адоптится без упоминания в системном промпте (едет на уже-используемом `get_object_structure`).

### Добавлено

- **`name_like` и `meta_type` у `get_object_structure`.** `name_like` — подстрока имени объекта (без префикса типа): сервер одним SQL по `metadata_objects` разворачивает критерий в набор и возвращает структуры ВСЕХ подходящих объектов за один вызов. `meta_type` (опц., RU/EN: `Catalog`/`Document`/`Документ`/…) сужает до одного вида метаданных. Сочетается с `sections=` (узкие секции на каждый объект). Ответ — `{matched, truncated, results:[...]}`. Защита от широкого критерия: потолок 50 объектов (`truncated=true`, если совпало больше — критерий уточняется). Общий хелпер `expand_object_criterion` — задел под ту же конвенцию на других объектно-ключевых инструментах. Регистр подстроки для кириллицы учитывается (SQLite `LIKE` case-insensitive только для ASCII).

## [0.40.0] — 2026-06-22

**Срез внутренних технических полей (внутренние id, хэши, таймстемпы) из MCP-ответов модели. Бесполезные модели поля удаляются в момент сериализации ответа — payload чище и стабильно короче, без зависимости от поведения модели. Переиндексация НЕ требуется (всё на serve-слое выдачи).**

> Контекст. В E2E-прогоне на УТ-11 `cache_read` составил ~90% счёта токенов — каждый ход перечитывает весь накопленный контекст, поэтому любое бесполезное поле в ответе инструмента амплифицируется на каждом ходу. Аудит выходных полей выделил класс «плумбинга» — внутренние идентификаторы строк/файлов, хэши узла/контента/AST и таймстемп индексации, которые не нужны модели ни в одном инструменте.

### Изменено

- **Внутренние техполя срезаются в горле сериализации ответа.** Хелпер `strip_plumbing_recursive` рекурсивно (по объектам и массивам — верхний уровень многих инструментов это `Vec<Record>`) удаляет ключи `id`, `file_id`, `node_hash`, `content_hash`, `ast_hash`, `indexed_at` из тела ответа. Вызывается в двух точках, покрывающих оба пути сериализации модель-facing ответов: `wrap_with_meta_extra` (обёртка `{result, _meta}` — `get_function`/`get_class`/`get_callers`/`get_callees`/`get_call_tree`/`find_symbol`/`get_imports`/`get_file_summary`/`read_file`/`list_files`/`grep_*`/`search_*` и др.) и `to_json` (плоские ответы без обёртки — `stat_file`/`get_stats`/`health`). Срез исполняется ДО пристёгивания `_meta`, поэтому `_meta.dependent_files`/`file_mtimes` (вход event-инвалидации кэша) не затрагиваются. На federation срез исполняется на каждой ноде локально (federation-форвард пробрасывает уже обрезанный JSON).
- **`stat_file` больше не возвращает `content_hash`/`indexed_at`** — поля `size`/`mtime`/`lines_total`/`language`/`exists`/`category` сохранены; описание инструмента обновлено.
- **Не затронуто:** поля `path`/`file_path`/`body`/сигнатуры, граф связей данных и вызовов, BSL-инструменты (их собственная обёртка строится из SQL-строк и плумбинга не несёт), механизм инвалидации кэша, индексация (демон сравнивает `content_hash`/`mtime` напрямую по SQLite, не по выдаче модели).

## [0.39.0] — 2026-06-18

**Демон больше не зависает при массовом git-обновлении 1С-репо. Изменение состава объектов (`Configuration.xml` в батче) пересобирает только лёгкий XML-слой обогащения, а не тяжёлый код-слой — переиндексация НЕ требуется (правка watcher-пути выдачи демона).**

> Контекст. При bulk-обновлении локального BSL-репо (`git reset --hard` / `pull` на далёкий коммит) watcher собирает батч с изменённым `Configuration.xml`. Раньше это запускало ПОЛНЫЙ `run_index_extras` прямо в watcher-цикле — пересбор `metadata_*` + `data_links` + `role_rights` + `code_usages` + `procedure_terms` (сотни тысяч процедур) + графа вызовов, в «живом» контексте конкурируя с `serve`-читателем за SQLite → busy-spin на 100% CPU на десятки минут. Воспроизводилось одинаково на ВМ (docker) и Windows.

### Исправлено

- **Изменение `Configuration.xml` больше не запускает полный re-enrichment в watcher-цикле.** В `run_incremental_extras` ветка `config_changed` (срабатывает при любом изменённом `Configuration.xml` — а он перезаписывается при каждой выгрузке `DumpConfigToFiles`, не только при реальной правке состава) раньше делала `return run_index_extras(...)` — полный тяжёлый проход. Теперь она пересобирает только **XML-слой** и НЕ делает `return`: тяжёлый код-слой держат точечные `update_*_for_file` по `.bsl` батча. На полной УТ (57K файлов) config-changed-батч из 43 `Configuration.xml` обрабатывается за **~21 сек** (XML-слой) вместо многоминутного зависания. Перечень объектов синхронизируется корректно — добавление/удаление/переименование объекта отражается в `metadata_objects` эквивалентно полному пересбору (3 регрессионных теста).

### Изменено

- **`run_index_extras` разделён на XML-слой и код-слой.** Новая функция `run_index_extras_metadata_layer` собирает перечень объектов, структуру (`attributes_json`), связи данных, конфиг-рёбра, права ролей, синонимы, формы, подписки, модули — обход XML выгрузки (секунды даже на УТ). Тяжёлый код-слой (`metadata_code_usages`, `procedure_terms`, `build_call_graph`) вызывается из полного `run_index_extras` следом, а на инкрементальном пути при изменении состава не трогается. Полный пересбор всего по-прежнему идёт при initial reindex и `index --force`. Поведение полного прохода не изменилось: порядок фаз сохранён, зависимости (attributes/synonyms → metadata_objects, call_graph → forms/subscriptions) соблюдены.

## [0.38.1] — 2026-06-17

**Демон не перестраивает enrichment-таблицы вхолостую при старте. Рестарт демона на неизменных данных — мгновенный (раньше каждый старт = полный пересбор `metadata_*`/terms/графа, минуты на больших конфигурациях).**

### Исправлено

- **Гейт против холостого re-enrichment на старте демона.** На старте после `full_reindex` (mtime fast-path) демон **безусловно** гонял полный `index_extras` — перестроение `metadata_objects`/`data_links`/`role_rights`/`code_usages`/`procedure_terms` (сотни тысяч процедур)/`forms`/`subscriptions`, даже когда mtime показал «0 изменений». На полной федерации (УТ/БП-СС/БП-ТДК/ЗУП) это ~15 минут на ровном месте при любом рестарте контейнера. Теперь `index_extras` пропускается, если: БД уже существовала, `full_reindex` записал 0 и удалил 0 файлов, и extras-таблицы процессора непусты (`LanguageProcessor::extras_present` — для BSL это непустые `metadata_objects` + механические термы в `procedure_enrichment`). Любое изменение данных, новая БД или пустые extras → полный проход как раньше; инкрементальные правки по-прежнему обрабатывает watcher-цикл через `index_extras_for_files`.
- **Ограничение:** гейт не отслеживает СХЕМУ extras. Если релиз добавил новую extras-таблицу, при неизменных данных она останется пустой — для таких релизов нужен разовый полный пересбор (`index --force` или ребилд БД). Зафиксировано в doc-комментарии `extras_present`.

## [0.38.0] — 2026-06-17

**Защита выдачи от disk-offload клиента. Тяжёлые ответы (карта большого модуля, длинные массивы значений/источников/реквизитов) обрезаются в источнике до сэмпла или компактной формы — с маркером полного числа — и не сбрасываются клиентом в файл с потерей хода. Переиндексация НЕ требуется (всё на serve-слое выдачи).**

> Контекст. Клиент (`claude` CLI / Claude Code) держит лимит на один `tool_result`, вливаемый inline в контекст (`MAX_MCP_OUTPUT_TOKENS` ≈ 25 000 токенов). Ответ сверх лимита harness **сбрасывает в файл на диск**, отдавая модели только путь + preview — после чего теряется структурный inline-доступ и модель грепает файл лишними ходами. Хард-капы ядра (`grep_*` 1 МБ, `read_file` 2 МБ) этот класс не ловят: там не громадная строка, а длинный массив (значения перечисления, источники подписок, карта функций большого модуля).

### Добавлено

- **`cap_response` — generic-страж размера ответа (serve-слой).** Пока сериализованный JSON превышает бюджет `[cap].max_response_bytes` (дефолт **48 000 байт** ≈ 12–24k токенов кириллицы, с запасом под 25k-токенный offload), повторно находит самый тяжёлый массив-значение-ключа и усекает его вдвое, оставляя рядом `<ключ>_total` (исходное число элементов, ставится один раз) и `<ключ>_truncated: true`. Усекаются **только массивы** — большие строки (контент `read_file`/`grep`) не трогаются. Гейт — список `[cap].cap_tools` (дефолт: `get_event_subscriptions`, `bsl_sql`, `find_references`, `get_register_writers`).
- **`[cap].cap_enabled` — глобальный выключатель `cap_response`** (дефолт `true`). Приоритетнее списка: при `false` cap не применяется ни к одному инструменту, что бы ни лежало в `cap_tools` (omit структурных и навигационный кап тела работают независимо). Нужен потому, что пустой `cap_tools` означает «дефолтный набор», а не «выключено».
- **`omit_oversize_sections` для структурных tools (`get_object_structure`).** Где массив/мапа — это ПОЛНЫЙ авторитетный ответ (структура объекта 1С), частичный сэмпл соврал бы («вот все значения перечисления»). Поэтому тяжёлая секция (массив > 1 элемента / мапа > 16 ключей) выкидывается ЦЕЛИКОМ с `<секция>_omitted: true` + `<секция>_count: N` — секция либо целиком в ответе, либо честно опущена с числом.
- **Навигационный кап тела (`get_function`/`get_class`).** Тело длиннее `[cap].max_function_body_chars` (дефолт **15 000 символов**) → навигационный стаб: голова + хвост + маркер + hint на `read_file(line_start,line_end)` / `grep_body`. Тело — связный код, поэтому режется не серединой, а головой+хвостом с точным диапазоном строк.
- **Конфиг `[cap]`:** `max_response_bytes`, `cap_tools`, `cap_enabled`, `max_function_body_chars` (все опциональны; 0 у байтовых/символьных порогов — выключить страж).

### Исправлено

- **`get_file_summary` на гигантских модулях больше не уходит в disk-offload.** `get_file_summary` — core-инструмент, оборачивается через `wrap_with_meta_extra`, где `cap` к ядру не применяется; добавление в `cap_tools` его не покрывало. На `УправлениеДоступомСлужебный` (972 функции, 47 399 строк) даже компактная карта (`MAP_DETAIL_CAP = 120` — только имена+строки, без сигнатур/docstring) = **100 164 символа** в одну строку → клиент сбрасывал ответ в файл, ход терялся (слабое место #4 прогона УТ-11, Q08 «RLS»). Теперь core сам зовёт `cap_response` перед обёрткой: остаётся sample + `functions_total` + `functions_truncated`, ответ возвращается inline.

### Замер (прогон УТ-11, Q08 «система прав доступа / RLS», Sonnet)

- На сборке с фиксом: **0** disk-offload за прогон, максимальный `tool_result` — **19 128 символов** (~4.8k токенов) при пороге offload ~25k токенов. 38 маркеров `*_truncated` в трейсе = штатная работа cap-механизмов. Вердикт по вопросу — ПОЛНОСТЬЮ.
- Smoke через federation (боевая УТ, `УправлениеДоступомСлужебный` 47 399 строк): `functions_total: 972`, `functions_truncated: true`, компактная карта — ответ inline (~48 КБ), offload устранён.

## [0.37.1] — 2026-06-16

**Детерминированные счётчики в BSL-tools + компактная выдача подписок по `source`. Модель цитирует готовое число вместо пересчёта массива (LLM занижает длинные списки), а `get_event_subscriptions` с фильтром больше не срывает лимит вывода. Переиндексация НЕ требуется.**

### Добавлено

- **`get_register_writers`: счётчики `writers_count` / `writes_to_count` + `*_count_by_type`** (по типу регистра). Раньше модель пересчитывала массив имён вручную и занижала (43 → «40»); теперь число готовое в ответе.
- **`get_object_structure`: секция `counts`** — число элементов каждой секции-массива (`tabular_sections`, `attributes`, `dimensions`, `resources`, `enum_values`…). Закрывает недосчёт табличных частей (10 → «5»).

### Изменено

- **`get_event_subscriptions` при заданном `source`: вместо полного `sources` — `sources_count` + `matches_source: true`.** У глобальных событий (`ПередЗаписью` и т.п.) подписки имеют `sources` до сотен типов (одна — 256); эхопечать раздувала ответ до 80К+ символов и срывала лимит вывода (ответ уходил в файл, терялся ход). Теперь по `source=РеализацияТоваровУслуг`: 80 183 → ~2 700 символов. Без фильтра — полный `sources`, как раньше.

## [0.37.0] — 2026-06-16

**Экономия токенов выдачи + надёжность резолва имён объектов 1С в BSL-tools. Компактный формат выдачи `grep_*`/`list_files`; одно-объектные BSL-tools принимают русские префиксы типов и не зависят от имени ключа; `find_symbol` терпим к синонимам имени. Изменения формата выдачи и резолва — переиндексация НЕ требуется.**

### Изменено

- **Компактный формат выдачи `grep_body`/`grep_code`/`grep_text`/`list_files` (ядро).** Вместо JSON-объектов с повторяющимися ключами (`{"line":N,"content":"X"}` на каждом из тысяч совпадений) — плоские строки, сгруппированные по файлу: `grep_text`/`grep_code` → `"N: content"`; `grep_body` → `"<имя> (<вид>) L<от>-<до>: <строки>(+N)"`; `list_files` → `"<path> | <язык> | <N> строк | <размер>"`. Структурный JSON-оверхед был основной статьёй расхода токенов (~55% ответа). `_meta` (dependent_files/file_mtimes) собирается отдельно и не дублируется. Дефолтный `limit` снижен 100 → 30.
- **`find_symbol`: описание заострено.** Голым именем звать только для уникального имени; для стандартных обработчиков/частых имён — сразу `path_glob` (иначе сотни локаций, `truncated`).

### Исправлено

- **BSL-tools принимают русские префиксы типов.** `get_object_structure`/`get_register_writers`/`get_data_links`/`find_data_path`/`find_references`/`get_object_profile`/`get_event_subscriptions`/`get_form_handlers`/`bsl_sql` с `object="Документ.X"` раньше возвращали пусто (в индексе типы только английские). Нормализация на входе через таблицу `META_FORMS` (`canonical_meta_type`/`normalize_object_ref`/`normalize_sql_object_refs`): `Документ.X` → `Document.X`; для `bsl_sql` — и единственное, и множественное число в литералах запроса. Устранён каскад впустую: пустой `get_register_writers` гнал лавину `read_file` по ManagerModule.
- **Одно-объектные BSL-tools не зависят от имени ключа.** `get_register_writers`/`get_data_links`/`find_references`/`get_object_profile`/`get_object_structure` берут значение первого непустого строкового аргумента, пропуская служебные ключи (`repo`/`depth`/`limit`/…) — модель больше не промахивается на `object` vs `full_name`.
- **`find_symbol` принимает `symbol`/`query` как алиасы `name` (ядро).** Раньше вызов с `symbol=…` падал с тёмной ошибкой разборщика «missing field `name`» (потерянный ход). `#[serde(alias)]` — синонимы подхватываются как `name`; контракт схемы (обязательный `name`) не изменён.

### Замер (УТ 11.5, бенчмарк-вопрос «жизненный цикл РеализацииТоваровУслуг»)

- Старый формат (baseline): **1 282 904** токенов / 33 turns.
- Компактный формат БЕЗ резолва имён: **2 172 388** / 48 (+69% — пустой `get_register_writers` гнал каскад `read_file`).
- Компактный формат + резолв имён (финал): **917 247** / 24, чистый повторный прогон **926 170** / 27, `retry=0`, вердикт ПОЛНОСТЬЮ — vs baseline **−28%**.

## [0.36.0] — 2026-06-14

**CORE B: квалификатор вызова сохраняется в графе. BSL хранит `callee` склеенно (`Модуль.Метод`) — единообразно с остальными языками; точная привязка вызовов общих и менеджер-модулей. Резолв direct-рёбер 52% → ~80-82%, ноль ложных привязок.**

> ⚠️ **Требуется полная переиндексация** (`index --force` по каждому репо): изменился формат `calls.callee` (BSL) и `proc_call_graph`. Mtime fast-path не подхватит. На федерации — синхронный ребилд всех нод.

### Изменено

- **CORE (ядро, влияет на все языки): BSL больше не выбрасывает квалификатор вызова.** При разборе `Модуль.Метод()` ядро раньше сохраняло в `calls.callee` только голое имя метода — квалификатор `Модуль` лежит соседним узлом в дереве onescript и игнорировался, из-за чего BSL был единственным языком, терявшим receiver. Теперь BSL склеивает `receiver.method` так же, как Python/JS/Go/… уже хранят `obj.method` — граф вызовов единообразен между языками. Голые локальные вызовы остаются голым именем. **Затрагивает выдачу `get_callers`/`get_callees`/`find_path`/`get_call_tree` и `find_path_bsl` для BSL: квалифицированные вызовы теперь отображаются как `Модуль.Метод`** (как `requests.get` у Python) — информативнее, но это изменение формата для BSL-запросов. Не-BSL языки не затронуты (подтверждено A/B на 6 языках Python/JS/TS/Java/Go/Rust — выдача `calls` байт-в-байт идентична).

### Добавлено

- **Резолв вызовов общих модулей по квалификатору (Tier C).** `ОбщегоНазначения.ЗначениеРеквизитаОбъекта` → точный адрес `…/CommonModules/ОбщегоНазначения/Ext/Module.bsl::ЗначениеРеквизитаОбъекта`. Снимает зависимость от эвристики «уникальный экспорт» (v0.35.0): имена, экспортные в ≥2 общих модулях, теперь резолвятся точно по квалификатору. На полной УТ: 88.3% вызовов общих модулей привязаны, ноль ложных.
- **Резолв вызовов менеджер-модулей по цепочке (Tier D).** `Справочники.Номенклатура.НайтиПоКоду` → `…/Catalogs/Номенклатура/Ext/ManagerModule.bsl::НайтиПоКоду`. Маппинг коллекция→папка выгрузки (`Справочники`→`Catalogs`, неправильные формы множественного числа учтены) — из единой таблицы `META_FORMS`. Платформенные методы менеджера (`ПустаяСсылка`, `НайтиПоКоду`), не экспортные в модуле объекта, корректно остаются NULL. На УТ: ~28 тыс. менеджер-вызовов привязано.
- **Прун объектных вызовов по квалификатору.** Склеенные `Объект.Метод`, где квалификатор — локальная переменная / объект платформы (`Запрос.Выполнить`, `Выборка.Следующий`, `НаборЗаписей.Записать`), удаляются из графа точнее списочного балласта: зная, что приёмник не модуль, режем даже коллизионные имена методов. Три защиты от потери реальных рёбер: щадим общие модули, коллекции метаданных (`Справочники`/`Документы`/…) и многоточечные цепочки-менеджеры; режем только нерезолвленные (`callee_proc_key IS NULL`). Объектный шум вычищен — `get_callees`/`get_call_tree` читаемые (раньше тонули в `Запрос.Выполнить`-листьях).

### Итог по резолву

- Полная УТ (57К файлов): резолв direct-рёбер **52.1% → 82.1%**, **ноль ложных привязок**. Федерация (УТ/БП-СС/БП-ТДК/ЗУП): **80-82%**.

### Тесты

- BSL: `resolves_callee_key_by_module_qualifier` (Tier C — коллизия разрешена квалификатором), `prunes_object_calls_protects_modules_collections_chains` (Tier D + прун — щадит модули/коллекции/цепочки, резолвит менеджер), обновлён `test_parse_bsl_calls` (склейка). 150 BSL + 277 CORE зелёные. Мультиязычный A/B (Python/JS/TS/Java/Go/Rust): граф не-BSL байт-в-байт идентичен старому бинарнику.

## [0.35.0] — 2026-06-14

**Починка графа вызовов BSL: одноимённые процедуры разведены по модулям, цель вызова резолвится в адрес, платформенный балласт отсеян. CORE: рёбра графа вызовов несут путь файла-источника.**

> ⚠️ **Требуется полная переиндексация** (`index --force` по каждому репо): изменился формат данных `proc_call_graph` и query-выдачи по `calls`. Mtime fast-path не подхватит — нужен форс. На федерации — синхронный ребилд всех нод.

### Исправлено

- **Граф вызовов больше не схлопывает одноимённые процедуры.** `caller_proc_key` в `proc_call_graph` теперь в формате `<rel_path>::<name>` (как `procedure_enrichment.proc_key`), а не голое имя — строится `JOIN calls ⋈ files`. На полной УТ 240 модулей с собственной `ОбработкаПроведения` перестали слипаться в 2 строки → 259 различных вызывателей. Раньше `find_path_bsl`/`bsl_sql` по графу не могли отличить документ.

### Добавлено

- **`callee_proc_key`: резолвер адреса цели вызова (этап 4e).** Две безопасные ступени: (а) локальный вызов — голое имя callee объявлено в файле вызывателя → `<file>::<callee>`; (б) уникальный экспорт — имя callee экспортно ровно в одном месте конфигурации (определяется по `Экспорт` в сигнатуре) → его адрес. Ядро при разборе вызова теряет квалификатор модуля (`Модуль.Метод` → `Метод`), но единственность цели снимает неоднозначность. Неоднозначное / динамическое (`Объект.Метод` по переменной) / платформенное → `NULL` (ложная привязка хуже честного NULL). На УТ резолвится 52% direct-рёбер.
- **Отсев платформенного балласта.** Рёбра в методы коллекций/объектов и глобальные функции платформы (`Вставить`/`Добавить`/`НСтр`/`Структура`…, цель вне кода конфигурации) удаляются из графа (~35%; на УТ 1.14М вставлено → 739К). Две защиты от потери реальных рёбер: удаляются только нерезолвленные (`callee_proc_key IS NULL`); имя, экспортное где-либо в конфигурации (`Записать`/`Получить`/`Удалить`…), не трогается вовсе (адаптивно к УТ/БП/ЗУП — каждая считает свой набор экспортов).
- **CORE: `get_callers`/`get_callees`/`find_path`/`get_call_tree` отдают путь файла-источника каждого ребра** (`path`, резолв `file_id → files.path`). Различает одноимённые функции из разных файлов — раньше выдача показывала голое имя + числовой `file_id` («N неотличимых строк»). Языко-нейтрально, переиндексации не требует (только query-слой).
- **`find_path_bsl`: обход по резолвленному адресу.** Между хопами связь через `COALESCE(callee_proc_key, callee_proc_name)` — по адресу цели, где он есть, иначе по сырому имени (нерезолвленный лист / синтетические рёбра). `from`/`to` принимают `<rel_path>::<name>` (для нерезолвленных листьев допустимо голое имя). В рёбрах пути добавлено `callee_key`.

### Тесты

- BSL: `resolves_callee_keys_local_unique_export_and_null` (оба tier + честный NULL), `prunes_platform_balast_keeps_real_and_resolved` (отсев + IS NULL guard + collision guard на экспортном имени), `incremental_direct_shared_edge_survives` переписан под path-семантику. CORE: 277 тестов зелёные (включая `get_call_tree` с `file_id` в CTE).
- Замер влияния на реиндекс (честный Rust A/B на полной УТ, 57К файлов): общее время 54.15с → 54.86с (+0.7с, шум), фаза построения графа 18.35с → 27.18с (+8.8с). Карточка rag-query #1524.

## [0.34.1] — 2026-06-13

**Диагностическое сообщение при недоступном демоне + фикс неверной документации про `CODE_INDEX_HOME` (issue #1).**

### Исправлено

- **Сообщение «демон не запущен / runtime-info отсутствует» теперь объясняет настоящую причину.** Процесс `serve` и демон находят друг друга только через `$CODE_INDEX_HOME/daemon.json`. Если у `serve` переменная не задана или указывает в другую папку, чем у демона, runtime-info не читается — и инструменты сообщали «Демон не запущен», хотя демон жив. Теперь сообщение называет ожидаемый путь к `daemon.json`, текущее значение `CODE_INDEX_HOME` и явно указывает частую причину: на Linux/macOS GUI-клиенты MCP (VS Code, Continue, Cline) **не читают `~/.bashrc`/`~/.zshrc`**, поэтому запущенный ими `serve` с пустым `env` не видит `CODE_INDEX_HOME` из шелла, а демон из терминала — видит. Решение — задать `CODE_INDEX_HOME` тем же абсолютным путём в секции `env` MCP-конфигурации клиента. Затронуты `client::base_url` (все data-tools и `daemon status`) и MCP-tool `health`. Воспроизведено на чистом Linux релизным бинарником. Issue #1 (автор @NorfLoud).
- **README/README_RU: убрано неверное утверждение про fallback.** Документация обещала, что при незаданном `CODE_INDEX_HOME` демон использует `%APPDATA%`/XDG — такого fallback в коде нет, переменная обязательна. Утверждение заменено на корректное + добавлен раздел Troubleshooting про рассинхрон `CODE_INDEX_HOME`.

## [0.34.0] — 2026-06-12

**Автоматический термовый fallback в `bsl_sql`: пустая выборка по таблицам процедур сразу возвращает выдачу `search_terms`, а не только hint.**

### Добавлено

- **`bsl_sql`: поле `terms_fallback` при пустой выборке по таблицам процедур** (`functions` / `proc_call_graph` / `procedure_enrichment`). Hint'ы v0.33 модели игнорируют (5 живых прогонов — 0 вызовов `search_terms`), поэтому при `row_count == 0` термовый поиск выполняется автоматически внутри того же вызова: слова берутся из строковых литералов SQL (`'%ПоШтрихкоду%'` → «по штрихкоду») и текстовых `params`, нормализация та же, что у термов (`split_identifier`: CamelCase-сплит, нижний регистр, ё→е, слова ≥3 символов), затем тот же триграммный FTS-запрос, что у `search_terms` (OR по словам, LIMIT 10). Ответ: `terms_fallback = {fts_query, results: [{proc_key, signature, score}]}`. Модель утилизирует данные «в руке» как обычный результат — живой прогон test03: пустой `IN ('ХранилищеОбщихНастроек', …)` → fallback вернул `ОбщегоНазначенияВызовСервера::ХранилищеСистемныхНастроекСохранить/Загрузить/Удалить`, и модель вписала их в отчёт как факт (hint в том же прогоне был снова проигнорирован).
- **Граница срабатывания:** только BSL-репо с наполненными таблицами обогащения (нет термов / старый индекс → молча без fallback, поведение прежнее); запросы не к таблицам процедур (например, `metadata_objects`) — прежний hint. При сработавшем fallback hint не добавляется — структура самодокументируется; при несработавшем — hint'ы v0.33 как были. На экзаменационных вопросах (196, rerun 12.06) fallback нейтрален — не сработал ни разу, его ниша — поиск кода по смыслу.
- **Известное ограничение:** в `results` поле `signature` — отпечаток механизма обогащения (`mech:v1`), не сигнатура процедуры.

### Тесты

- Юнит-тесты: `sql_string_literals` (экранирование `''`, wildcards), `searched_proc_tables`, `terms_fallback_for_sql` (находка по литералам и по text-params; `None` без термов/без слов ≥3 символов).

## [0.33.0] — 2026-06-11

**Пустой поиск процедуры на BSL-репо подсказывает search_terms — для search_function, grep_body, grep_code, find_symbol.**

### Добавлено

- **Пустой результат поиска процедуры на BSL-репо подсказывает `search_terms`** — у `search_function`, `grep_body`, `grep_code` и `find_symbol`. По per-call анализу бенча 11.06 (10 бизнес-задач на УТ-11): модель для «поиска процедуры по смыслу» рефлекторно берёт точный поиск (по имени или тексту) и до `search_terms` не доходит вообще (0 вызовов за прогон), а остаточные пустые ходы — ровно его ниша (обработчики в общих модулях: префиксация, значения по умолчанию, коллизии обмена — grep по модулям объекта даёт 0, потому что код живёт в БСП-модуле и привязан подпиской). Живой прогон test03 показал серию из трёх подряд пустых `grep_body` (ходы 17/20/21), ни один из которых не звал в `search_terms` — поэтому hint навешен на КАЖДЫЙ пустой ответ этих инструментов, а не только на `search_function`. Тот же приём, что сработал в 0.31 (hint'ы ломают серию слепых повторов). На не-BSL репо hint'ы прежние; `search_class`/`grep_text` не трогались (термы индексируют процедуры, не классы и не xml/текст).
- **Пустая выборка `bsl_sql` (0 строк) тоже подсказывает.** Живой прогон показал, что Opus для «поиска процедуры по смыслу» устойчиво предпочитает `bsl_sql` (LIKE по `metadata_objects`/`functions`), а не search_function/grep — и в search_terms не уходит. Поэтому при `row_count == 0`: если запрос обращался к таблицам процедур (`functions`/`proc_call_graph`/`procedure_enrichment`) — hint зовёт в `search_terms`; иначе общий hint (проверьте фильтры; кириллица в `LIKE`/`=` регистрозависима — SQLite `lower()` её не сворачивает). Это закрывает точку, где модель реально застревала (test03 ходы 10/14 — пустой `bsl_sql` по `functions` без направления дальше).
- **Известное ограничение:** hint срабатывает на ПУСТОМ результате. Если точный поиск вернул непустой, но нерелевантный «мусор» (например, `search_function` по частому слову даёт десятки совпадений-однофамильцев) — подсказка не появляется, т.к. инструмент не может оценить релевантность (это делает модель). Этот сценарий частично закрыт упоминанием `search_terms` в description инструментов.
- **Замечание по эффекту:** на Opus за несколько живых прогонов (test03/test05) модель в `search_terms` так и не пошла — решала задачу через `bsl_sql` по синонимам объектов (тот же смысловой мост, что и термы). Hint'ы корректны и не вредят, могут помочь другим моделям/стратегиям, но «волшебной пулей» на Opus не стали — поведенческий эффект не зафиксирован.

## [0.32.0] — 2026-06-11

**Новые секции структуры объекта (owners/value_types/properties/enum_synonyms/commands + synonym/required реквизитов), рёбра owner и functional_option_content в data_links, роли в metadata_objects, brace-альтернативы `{a,b}` в path_glob, hint LIMIT в bsl_sql. Фикс типов «—» у ОпределяемыйТип.**

### Добавлено

- **`get_object_structure`: пять новых секций структуры** (по итогам прогона 747 вопросов 1С:Проф на УТ-11):
  - `owners` — владельцы подчинённого справочника из `<Owners>` (`Catalog.ЭквайринговыеТерминалы` → `Catalog.ДоговорыЭквайринга`);
  - `value_types` — тип значения характеристик ПВХ / константы из корневого `<Type>`: для ПВХ это список доступных аналитик (`СтатьиДоходов` → 8 типов);
  - `properties` — скалярные свойства шапки по белому списку: периодичность/режим записи ИР (`ЦеныНоменклатуры25` → `Periodicity=Second`), вид регистра накопления, нумерация документа, иерархия/длины кодов справочника;
  - `enum_synonyms` — UI-подписи значений перечисления отдельной картой, формат `enum_values` не менялся (`ЗакупкаПоИмпорту` → «Импорт»; 814 подписей у `ХозяйственныеОперации`);
  - `commands` — команды объекта `[{name, synonym?}]` из `<ChildObjects>/<Command>`: «Создать на основании», печатные формы и т.п.
- **У реквизитов в `attributes_json` — `synonym` (UI-подпись, ru-приоритет) и `required`** (`<FillChecking>ShowError`): «какое поле обязательно в Х» теперь отвечается без XML.
- **`data_links`: два новых вида рёбер** — `owner` (подчинённый справочник → владелец) и `functional_option_content` (функциональная опция → объекты её состава из `<Content>`; `ИспользоватьЛимитыРасходаДенежныхСредств` → 3 объекта).
- **Роли в `metadata_objects`**: `Role` добавлен в известные типы метаданных — 1288 ролей УТ-11 с синонимами доступны через `bsl_sql`/поиск по synonym.
- **Brace-альтернативы `{a,b}` в `path_glob`/`pattern`** (`grep_code`/`grep_text`/`grep_body`/`list_files`): SQLite GLOB не поддерживает альтернацию, и `**/*.{bsl,xml}` молча возвращал пустоту — теперь паттерн раскрывается в OR-группу GLOB-условий (`expand_glob_braces`, до 64 вариантов, без вложенности — как в globset).
- **`bsl_sql`: hint при совпадении числа строк с LIMIT из текста запроса** — «выдача, возможно, обрезана вашим SQL LIMIT» (раньше агент принимал обрезанную выдачу за полную).

### Исправлено

- **Типы-`ОпределяемыйТип` отдавались как «—»** в структуре объекта: `DefinedType` сериализуется в выгрузке как `<v8:TypeSet>`, а парсер ловил только теги `:Type`. Теперь `ИНН` → `ОпределяемыйТип.ИНН`.

### Тесты

- Юниты: парсинг owners/TypeSet/properties/FillChecking/синонимов/value_types/enum_synonyms/commands, `expand_glob_braces` (декартово произведение, вложенность/незакрытая скобка — литерально), `sql_limit_value`. Полный workspace зелёный.
- Smoke на живой УТ-11 (ut-test, 57 102 файла): 11/11 проверок зелёные; полная force-переиндексация — 2 мин 23 сек (сервисы на время переиндексации останавливать — конкуренция за SQLite замедляет на порядок).

## [0.31.0] — 2026-06-11

**Фикс «слепого» `get_form_handlers`, фильтр `source` и отказ на неизвестные параметры в `get_event_subscriptions`, hint'ы на пустых ответах графовых и файловых инструментов.**

### Исправлено

- **`get_form_handlers` не находил НИ ОДНОЙ формы на боевых конфигурациях.** Инструмент искал точным сравнением `owner_full_name = 'Document.X'` (как в его же документации), а в БД значения хранятся в формате папки выгрузки — `'Documents.X'` (множественное число; на УТ-11 1350 строк в plural, 0 в singular). Теперь принимаются оба формата: точный матч, при промахе — повтор с конвертацией `<Singular>.<Name>` → `<PluralFolder>.<Name>` (общий хелпер `meta_type_to_folder`, вынесен из `get_object_profile`); в ответе — реально сматченный ключ БД.
- **Текст ошибки битого regex в `grep_body`/`grep_code`** читался как «Invalid parameter name: regex parse error…» (артефакт маппинга ошибки компиляции в `rusqlite::Error::InvalidParameterName`) и сбивал агента на поиск «неверного имени параметра». Теперь `UserFunctionError`: «grep_body: regex parse error…».

### Добавлено

- **`get_event_subscriptions`: фильтр `source`** — подписки по объекту-источнику. Принимает `'Document.ЗаказКлиента'`, `'DocumentObject.ЗаказКлиента'` или короткое имя `'ЗаказКлиента'`; регистр не важен; тип `Document` автоматически матчит `DocumentObject` из `sources_json`.
- **`get_event_subscriptions`: неизвестные параметры отклоняются** с перечнем допустимых фильтров. Раньше `object=…` молча игнорировался, и инструмент отдавал ВСЕ подписки (~52K токенов в контекст агента вместо указания на ошибку).
- **Умная ошибка `get_form_handlers`**: форма не найдена, но владелец существует → в ответе `available_forms` (реальные формы владельца); владельца нет → hint про формат owner и проверку через `get_object_structure`/`bsl_sql`.
- **Hint'ы на пустых ответах** (раньше — голый `{"result":[]}`, модель повторяла тот же вызов): `get_callers`/`get_callees` (имя должно быть точным, без скобок и владельца; пусто также если вызовов реально нет), `list_files` (pattern — glob от корня репо), `get_imports` (file_id: нет import-конструкций — для BSL норма; module: это ИМЯ импортируемого модуля, не путь к файлу).

### Изменено

- **`get_event_subscriptions`: default limit 200 → 50.** Безфильтровый вызов на УТ-11 отдавал ~52K токенов; `truncated`+`total` в ответе подсказывают сузить фильтр или дослать больший limit. MAX_LIMIT (2000) не менялся.
- Hint пустого `get_function`/`get_class` упоминает и `search_class` (раньше — только `search_function`).
- `bsl_sql` description: задокументирован формат `metadata_forms.owner_full_name` = `'<PluralFolder>.<Name>'` (`'Documents.ЗаказКлиента'`) — по аналогии с `metadata_modules.object_name`.

## [0.30.0] — 2026-06-11

**Механическое обогащение термов при индексации (без LLM) + триграммный FTS: `search_terms` впервые рабочий на боевых конфигурациях. Умные ошибки `bsl_sql`.**

### Добавлено

- **Механическое наполнение `procedure_enrichment.terms` при индексации** — новый модуль `terms.rs` и проход `index_procedure_terms` (+ per-file инкремент). Для каждой процедуры термы собираются из четырёх дешёвых источников: слова имени процедуры (CamelCase/подчёркивания/смена алфавита: УточнитьДанныеПоШтрихкоду → «уточнить данные по штрихкоду»), слова имени объекта-владельца (из пути модуля), СИНОНИМ объекта-владельца из `metadata_objects.synonym` (механический мост «русское представление ↔ английский идентификатор»), комментарий над процедурой. Без LLM: 259 414 процедур УТ-11 обогащаются за доли 63-секундного полного ребилда. Подпись `mech:v1`; записи LLM-прохода `enrich` (другая подпись) не перетираются. Ё→Е сворачивается при записи.
- **Триграммный токенайзер FTS термов** (`tokenize='trigram'` вместо unicode61) — словоформы и подстроки от 3 символов («штрихкод» находит «ПоШтрихкоду»), регистр и ё/е не важны. Существующие БД мигрируются автоматически (`ensure_trigram_tokenizer`: drop + rebuild из content-таблицы при несовпадении DDL).
- **Умные ошибки `bsl_sql`** — при `no such column/table` ответ дополняется `did_you_mean` (Левенштейн), `column_exists_in_tables` (в каких таблицах колонка реально есть — ловит путаницу meta_type/module_type) и колонками таблиц запроса. Ошибка становится самоисправляющейся за тот же ход (по находке бенча: голая ошибка стоила агенту хода разведки).

### Изменено

- **`search_terms` переписан под механические термы**: многословный запрос без явных операторов серверно переписывается в OR по словам (неявный AND на коротких термах почти всегда давал 0 — находка бенча), слова короче 3 символов отбрасываются, ё→е сворачивается, переписанный запрос виден в ответе (`fts_query`). Новое описание («ПЕРВЫЙ выбор для поиска функционала», как спрашивать), два контекстных hint'а при пустом результате.
- **Исправлен фильтр репо в `search_terms`** — биндился алиас маршрутизации вместо `'default'` (как во всех BSL-tools): инструмент возвращал пустоту даже при наполненной таблице. Баг жил незамеченным, пока таблица была пустой.
- **Тексты ошибок missing-parameter** у `get_function`/`get_class`/`get_object_structure` больше не подсказывают `names`/`full_names`: при выключенном mass-mode подсказка вела модель в отбиваемый вызов (наблюдение бенча).

### Тесты

- Юниты `terms.rs` (сплит кириллица/латиница/аббревиатуры/цифры, ё→е, объект из пути, комментарии), `index_procedure_terms` (механика + LLM-защита + инкремент с чисткой), `ensure_trigram_tokenizer` (миграция + substring-матчи), OR-rewrite и ё в `search_terms` (интеграционный), `enrich_prepare_error` (колонка и таблица). Полный workspace зелёный.
- E2E-бенчи на ut-test (10 бизнес-задач, Opus headless): спиральный кейс «где функционал X» решается одним вызовом `search_terms` вместо ~22 лексических попыток; срез без `bsl_sql` подтвердил его ценность (+33% ходов, +19% стоимости без него).

## [0.29.0] — 2026-06-10

**Синонимы всех объектов метаданных; узкая выборка `sections` в `get_object_structure`; columnar-формат `bsl_sql`.**

### Добавлено

- **Синонимы (русские представления) ВСЕХ объектов метаданных.** Новый лёгкий проход индексации `index_object_synonyms` заполняет `metadata_objects.synonym` для всех типов объектов — включая `CommonModule`/`Constant`/`CommonPicture`/`FunctionalOption` и прочие без структуры реквизитов, не входящие в `OBJECT_FOLDERS` (раньше синоним был только у объектов со структурой). Парсер `parse_object_header_xml` читает только шапку корневого XML (meta_type/Name/Synonym) и прерывается на `<ChildObjects>` — проход дёшев. Приоритет `v8:lang=ru`, иначе первое непустое представление; синоним базовой конфигурации не перетирается расширением. Зачем: синоним — механический мост «русское представление ↔ английский идентификатор» для смыслового поиска без LLM-обогащения.
- **Параметр `sections` в `get_object_structure`** — узкая выборка секций структуры (как `sections` у `get_object_profile`): вернуть ТОЛЬКО указанные ключи из `attributes`/`tabular_sections`/`dimensions`/`resources`/`posting`/`enum_values`/`predefined`. Без параметра — все секции (обратная совместимость). Работает и в одиночном (`full_name`), и в массовом (`full_names`) режиме. Рычаг экономии контекста: `["posting"]` — поведение проведения ~0.2 КБ вместо полного объекта; `["attributes"]` — реквизиты шапки без табличных частей; `["dimensions","resources"]` — для регистров.

### Изменено

- **`bsl_sql`: columnar-формат результата.** `rows` теперь массивы значений по позициям `columns` вместо JSON-объектов `{колонка: значение}` — имена колонок не дублируются в каждой строке, экономия контекста на широких выборках. Формат явно описан в description инструмента.
- **Смягчена формулировка массового режима** в описаниях `get_function`/`get_class`/`get_object_structure` и параметров `names`/`full_names`: батчить ТОЛЬКО когда заведомо нужен весь набор и результат одного элемента не отменит надобность в остальных; при отборе кандидатов — по одному с остановкой по ходу; «сомневаешься — по одному». Кодирует вывод бенча ut-test из 0.28.0 (front-load токенов и over-fetch от безусловного батчинга). Актуально для конфигураций с включённым `[mcp].mass_mode_tools`.

### Документация

- `docs/operations.md` — операторские процедуры администрирования индексатора (добавление репо в daemon.toml+serve.toml, hot-reload daemon config, рестарт/ребилд, диагностика «MCP не отвечает»), вынесены из правил сессии.

### Тесты

- `parse_object_header_xml` (ru-приоритет синонима, break на `<ChildObjects>`, объект без синонима), `apply_sections_filters_top_level_keys` (None/пустой список/не-объект — без изменений), columnar-тесты `collect_rows_*`. Полный workspace зелёный.

## [0.28.0] — 2026-06-10

**Массовый режим (`names[]`/`full_names[]`) по умолчанию ВЫКЛЮЧЕН; включается белым списком `[mcp].mass_mode_tools` в `daemon.toml`.**

### Изменено (поведение по умолчанию)

- **Массовый режим инструментов переведён в opt-in и по умолчанию выключен у всех.** Бенчмарк на ut-test (10 бизнес-задач, Opus, ci-рукав новыми инструментами против baseline) показал, что обещанная экономия токенов от пачки **не подтверждается**: суммарно input-токены выросли (+37% на прогоне), хотя turns почти не изменились. Механика: пачка *front-load'ит* данные (все цели приходят на первый ход и перечитываются на каждом последующем), провоцирует over-fetch (модель тянет больше целей, чем достала бы поштучно с ранней остановкой), а на «горячих» (неуникальных) именах `get_function`/`get_class` ответ раздувается неограниченно. Токены — главная статья расхода на подписке, поэтому массовый режим по умолчанию отключён.
  - Управление — новая секция `daemon.toml`:
    ```toml
    [mcp]
    # инструмент в списке публикует plural-параметр (модель сама решает, батчить ли);
    # нет в списке → сервер не предлагает пачку. Пусто/отсутствует → выключено у всех.
    mass_mode_tools = ["get_object_structure"]
    ```
  - Инструмент **в списке** публикует `names[]`/`full_names[]` в `tools/list` (модель видит и может батчить). **Не в списке** — сервер убирает plural-параметр из схемы (`list_tools`) и отбивает `tools/call` с ним (`-32602 Invalid params`, двойная защита).
  - **Совместимость:** одиночные `name`/`full_name` работают как прежде. Это поведенческое изменение относительно 0.26.0/0.27.0, где массовый режим был включён у всех трёх инструментов. Чтобы вернуть прежнее — `mass_mode_tools = ["get_object_structure", "get_function", "get_class"]`.

### Тесты

- `strip_mass_mode_param` (удаление plural-параметра из схемы + обрезка описания), `apply_mass_mode_tools` (пустой список → выкл; белый список → членство), парсинг `[mcp].mass_mode_tools` и дефолт-пусто. `cargo test`: code-index-core зелёный.

## [0.27.0] — 2026-06-10

**Массовый режим исполняется параллельно: элементы `names[]`/`full_names[]` обрабатываются конкуррентно, а не циклом.**

### Изменено

- **Mass-mode `get_function`/`get_class`/`get_object_structure` исполняется ПАРАЛЛЕЛЬНО.** Раньше — последовательный цикл с `await` на каждом элементе (а в `get_object_structure` — `map` на одном соединении). Теперь каждый элемент берёт своё read-only соединение из `StoragePool` и исполняется в `spawn_blocking` — синхронный rusqlite не блокирует общий async-runtime, параллелизм естественно ограничен семафором пула (`pool_size`, default 4). Формат ответа НЕ менялся: `{results:[...]}` строго в порядке запроса, пер-элементные `{error}` сохранены (битый элемент не валит батч), `_meta` с элементов срезается как раньше. Одиночные `name`/`full_name` — путь без изменений. Внутри: общий хелпер `mcp::tools::mass_map` (checkout из пула + `spawn_blocking` + сборка по порядку), из `get_function`/`get_class` выделены sync-ядра `get_function_with`/`get_class_with`, в bsl-extension замыкание `resolve_one` стало свободной fn. Смок на живом serve (`oleg`, КА 1.1, локально): батч 4 тяжёлых объектов — 3.0 мс против 19.7 мс суммы одиночных.

### Тесты

- `mass_map_runs_concurrently_and_preserves_order` (4 элемента по 100 мс на пуле из 4 соединений — wall < 250 мс, порядок цел), `mass_map_on_single_pool_stays_correct` (пул из 1 — деградация до последовательного без потери корректности), `get_object_structure_batch_non_string_element` (нестроковый элемент → `{error}` на своей позиции), `get_object_structure_batch_empty_list`. `cargo test`: code-index-core 267 + bsl-extension 19, 0 failed.

## [0.26.0] — 2026-06-10

**Массовый режим инструментов: структуры/тела нескольких объектов одним вызовом (`get_object_structure`, `get_function`, `get_class`).**

### Добавлено

- **Параметры `names: [...]` в `get_function` и `get_class`** — тела нескольких функций/классов одним вызовом вместо серии одиночных. Ответ — `{results: [...]}` в порядке запроса (каждый элемент — `{result: [...записи...], hint?}` без служебного `_meta`); ненайденное имя даёт пустой `result` + `hint` в своём слоте, не валит батч. Одиночный `name` без изменений (обратная совместимость). `find_symbol` намеренно не затронут (остаётся одиночным — у него свой `NameParams`). Кандидаты выбраны по статистике серий реального прогона (`get_function` — 2-й по числу группируемых вызовов после `get_object_structure`). Тяжёлые `bsl_sql`/`get_object_profile` массовыми НЕ делаются: у них высокое переиспользование, блоб-кэш закопал бы дорогой пер-объектный кэш (нужен слой растворения — отдельная задача).
- **Параметр `full_names: [...]` в `get_object_structure`** — запрос структуры нескольких объектов одним вызовом вместо серии одиночных. Ответ — `{results: [...]}` в порядке запроса; несуществующий объект даёт `error` + `did_you_mean` в своём слоте и не валит остальной батч. Одиночный `full_name` работает как прежде (обратная совместимость). Зачем: на задачах «структуры этих N документов/справочников/регистров» модель группирует независимые объекты в один вызов — меньше round-trip'ов, меньше перечитывания истории (главная статья расхода токенов). Обработка элементов — последовательный цикл на одном соединении (`get_object_structure` — дешёвый SELECT по индексу, параллелизм избыточен). Зонд на ut-test (Opus, headless): модель сама осваивает массовый режим из описания инструмента — в 3/3 задачах прислала `full_names` пачкой (4 документа / 5 справочников / 3 регистра) без подсказки про формат параметра.

### Тесты

- Новый интеграционный тест `get_object_structure_batch_full_names` (3 объекта: 2 существуют + 1 нет — порядок, структура, graceful error в слоте). `bsl-extension` зелёный (17 тестов, 0 failed). Смок на живом MCP (ut-test) подтвердил массовый режим.

## [0.25.0] — 2026-06-09

**Свойства проведения документа в `get_object_structure`; точность графа вызовов BSL; срезка токенов на горячих именах; устранение ложного `indexing`.**

### Добавлено

- **Секция `posting` в `get_object_structure`/`get_object_profile`** — свойства проведения документа из корневого `<Properties>`: `Posting`, `RealTimePosting`, `RegisterRecordsDeletion`, `RegisterRecordsWritingOnPost`. Только у документов (у прочих объектов секции нет). Раньше эти свойства не индексировались — агент не мог узнать поведение при проведении/отмене движений и уходил в догадки (на бизнес-задачах УТ: «что с движениями при отмене?» → предположение вместо факта). Смок на ut-test: `Document.РеализацияТоваровУслуг` → `posting: {Posting: Allow, RealTimePosting: Deny, RegisterRecordsDeletion: AutoDeleteOff, RegisterRecordsWritingOnPost: WriteSelected}`.

### Исправлено

- **Граф вызовов BSL ловит вызовы функций в выражениях.** `get_callers`/`get_callees` молча возвращали единицы рёбер вместо тысяч: обходчик парсера ловил только `call_statement` (вызов-процедуру как оператор), а вызовы функций в выражениях (`Рез = Модуль.Функция(...)` — узлы `method_call` внутри присваивания/условия/аргументов) терялись целиком. Переписаны `visit_body_for_calls` + `visit_node` (хелпер `record_method_call`). На ut-test (УТ 11.5): `get_callers(ЗначениеРеквизитаОбъекта)` 1 → 4560 рёбер; `proc_call_graph` direct 458011 → 790835.
- **Ложный статус `indexing` от `path_status` демона.** Раньше `std::fs::canonicalize()` звался на КАЖДЫЙ запрос — FS-зависимо и под нагрузкой реиндексации соседних репо промахивался, отдавая ложный `indexing` на готовое репо. Теперь FS-free сопоставление по нормализованному ключу (симметрично `/health`): точное совпадение либо ближайший родитель — самый длинный tracked-ключ-префикс.

### Изменено (срезка контекста на больших репо)

- **Cap локаций (`LOCATION_CAP=50`)** в `find_symbol`/`get_function`/`get_class`: на сверхгорячем имени (352 определения `ОбработкаПроведения`) выдача локаций 32K → 5.3K токенов (−84%); при обрезке — `{truncated, total, shown}`. Уникальное имя + `path_glob` по-прежнему отдаёт тело.
- **Дедупликация `get_call_tree`** (`expanded: HashSet`): узел с многими родителями разворачивается один раз (повтор → `{name, repeated:true}`). callers depth=5: 178K → 8.4K токенов (−95%).
- **`grep_body` при 0 совпадений** подсказывает: для составного имени `Объект.Поле` взять короткий якорь (только `Объект` или только `Поле`) либо regex с гибким пробелом (`Объект\s*\.\s*Поле`) — идентификатор бывает разорван форматированием или сидит в тексте запроса.
- Описания tool-ов в `mcp/mod.rs` синхронизированы с поведением: `search_*`/`find_symbol` отдают локации без тел; `get_function`/`get_class` при множестве совпадений опускают тела и просят уточнить `path_glob`.

### Тесты

- Весь workspace зелёный (265 тестов, 0 failed). Новые юнит-тесты: парсинг секции `posting`, отсутствие секции у не-документов. Полная переиндексация ut-test (57102 файла) + смок на живом MCP подтвердили `posting` и граф вызовов.

## [0.24.0] — 2026-06-08

**Пул read-only соединений на репозиторий в `serve`: запросы к одному репо больше не сериализуются на едином мьютексе.**

### Добавлено

- **Пул соединений на репозиторий (`storage::pool::StoragePool`).** Раньше каждый репо в `serve` обслуживался одним `rusqlite::Connection` под `tokio::sync::Mutex` — любой tool брал мьютекс на всё время обработки, поэтому тяжёлый запрос (`bsl_sql` до 8 c, полный `grep_code`, рекурсивные `find_path`/`get_call_tree`) задерживал ВСЕ остальные запросы к ТОМУ ЖЕ репо, включая мгновенный `get_function`. Теперь `serve` держит несколько read-only соединений к одному `index.db` и читает индекс параллельно (SQLite в режиме WAL рассчитан на много читателей). Соединения открываются лениво до `pool_size`, возвращаются в пул по Drop guard'а; число одновременно выданных соединений ограничено семафором. Не затрагивает данные/результаты — только конкурентность.
- **Секция `[pool]` в `serve.toml`** — три опциональных параметра (дефолты в скобках): `pool_size` (4) — число соединений на репо; `per_conn_cache_kib` (16384 = 16 МБ) — page-cache на соединение; `busy_timeout_ms` (5000) — ожидание при кратких блокировках во время checkpoint/backup демоном. Отсутствие секции или отдельных полей → дефолты; `0` санитизируется (`pool_size`→1, `cache`→дефолт). **Дефолт по памяти нейтрален:** 4 × 16 МБ = 64 МБ на активный репо — столько же, сколько у прежнего единственного соединения (`cache_size=-64000`).
- **`busy_timeout` на read-only соединениях** (раньше не выставлялся → по умолчанию 0): краткая `SQLITE_BUSY` в окне checkpoint/backup демоном теперь переждётся, а не превратится в ошибку.

### Изменено

- Поле `RepoEntry.storage`: `Option<Arc<Mutex<Storage>>>` → `Option<Arc<StoragePool>>`; метод `local_storage()` → `storage_pool()`. Tool-хендлеры (core `mcp::tools::*` и все BSL-tools расширения) берут соединение через `pool.get().await` вместо `lock().await`. Внутреннее изменение — на MCP-контракт (`tools/list`, формат ответов) не влияет.
- `CodeIndexServer::from_federated` принимает `PoolConfig` (из `serve.toml [pool]`); конструкторы моно-режима и тестов используют дефолтный `PoolConfig` / одно-соединённый пул (`StoragePool::single`).

### Тесты

- Юнит-тесты пула: переиспользование соединений, санитизация `0`, single-mode; **«тяжёлый держатель не блокирует второй запрос» при `pool_size>=2`** и контраст **«пул из 1 соединения сериализует»**. Весь workspace зелёный (262+ тестов).

## [0.23.0] — 2026-06-08

**Универсальный граф вызовов: рекурсивный путь `find_path` и дерево `get_call_tree` по таблице `calls` (любой язык). BSL-специфичный `find_path` переименован в `find_path_bsl`.**

### Добавлено

- **MCP-tool `find_path(repo, from, to, max_depth=5, language?)`** — кратчайший путь в графе вызовов от функции `from` до `to` (итеративный cycle-safe BFS по уникальным узлам таблицы `calls`, `max_depth` в [1..10]). Универсальный, любой язык — раньше рекурсивный обход пути был только в BSL-расширении (`proc_call_graph`), теперь есть и для ядра (`code-index`). Возвращает `{from, to, found, path: [{caller, callee, line}], max_depth}`. На пустом результате — `hint`.
- **MCP-tool `get_call_tree(repo, root, direction='callees', max_depth=3, max_nodes=200, language?)`** — дерево вызовов от функции `root` на глубину `max_depth`. `direction`: `callees`/`down` (что вызывает root вглубь) или `callers`/`up` (кто вызывает root). Раньше из ядра был доступен только один уровень (`get_callers`/`get_callees`). Возвращает плоский список рёбер `[{caller, callee, line, depth}]` и вложенное дерево `{name, children}`. При достижении `max_nodes` — `truncated=true`.
- Federation-routes `/federate/find_path` и `/federate/get_call_tree`; типы `CallEdge`/`CallTreeEdge` в `storage::models`; storage-методы `find_call_path` (итеративный cycle-safe BFS по УНИКАЛЬНЫМ узлам — каждый узел разворачивается раз, без взрыва на циклах/кратных рёбрах) и `get_call_tree` (recursive CTE), seek по `idx_calls_caller`/`idx_calls_callee`. Юнит-тесты на прямое ребро, два прыжка, предел глубины, фильтр по языку, направления дерева и обрезку по `max_nodes`.

### Совместимость

- **BSL-tool `find_path` переименован в `find_path_bsl`** (модуль `find_path_bsl.rs`, структура `FindPathBslTool`). Поведение и параметры (`from`, `to`, `max_depth`, по `proc_call_graph` с `call_type`) не изменились — только имя. Имя `find_path` теперь занято универсальным tool'ом ядра. Клиенты, вызывавшие BSL `find_path` напрямую, должны перейти на `find_path_bsl`.
- На сборке `bsl-indexer` в federation-режиме `tools/list` возвращает на 2 инструмента больше (универсальные `find_path` + `get_call_tree`); набор BSL-tools по числу не изменился (переименование).

## [0.22.0] — 2026-06-08

**Кириллица в `bsl_sql` и графовых tools (регистронезависимый поиск по русским именам) + нечёткий пословный FTS-поиск функций/классов + облегчённый payload поиска + параметр `sections` у `get_object_profile`.**

### Исправлено

- **`lower()`/`upper()` в SQLite теперь понимают кириллицу.** Встроенные функции SQLite складывают регистр только латиницы, поэтому в `bsl_sql` запрос `WHERE lower(name) LIKE '%эдо%'` по русским именам метаданных возвращал пусто, и агент откатывался на перебор объектов. Регистрируем Unicode-aware `lower`/`upper` (Rust `String::to_lowercase`/`to_uppercase`), перекрывая встроенные, на всех путях открытия БД (`register_sql_functions` рядом с `register_regexp`). Проверено на боевой УТ-11: `lower('ЭДО')='эдо'`, срез `WHERE lower(name) LIKE '%эдо%'` по `metadata_objects` — 0 → 336 объектов.

### Добавлено

- **Регистронезависимый реверс-поиск по кириллице в `find_references`.** Колонки `data_links.to_object_key` (= `lower(to_object)`) и `role_rights.object_name_key` (= `lower(object_name)`), посчитанные в Rust (SQLite-`lower()` кириллицу не берёт), + индексы `idx_dl_to_key` / `idx_rr_object_key`. `find_references` (`data_refs` / `role_rights`) находит ссылки на объект независимо от регистра русского имени. Заполняются при наполнении/инкрементальном обновлении графа.
- **Параметр `sections` у `get_object_profile`** — `['structure'|'forms'|'modules'|'data_links']` сужает ответ (рычаг удешевления: `['structure']` вернёт только реквизиты/ТЧ/измерения/ресурсы без форм, модулей и связей данных).

### Изменено

- **Нечёткий FTS-поиск `search_function`/`search_class` по СЛОВАМ.** OR между словами запроса, префиксные термы, ранжирование bm25 (имя важнее `qualified_name`/docstring/тела). Принимает многословное описание («расчёт цены продажи реализация»), не требует одного точного идентификатора; при 0 совпадений — поле `hint`. Нормализация запроса — `sanitize_fts_query`.
- **Облегчённый payload `search_function`/`search_class`** — без тел функций/классов: только имя, `qualified_name`, путь, диапазон строк, сигнатура, обрезанный docstring (200 симв.), `override_*`. Раньше до 20 результатов с полными телами раздували ответ в десятки тысяч символов.
- **Компактная карта `get_file_summary`** при > 120 функций в файле — имена + строки + `override_type` без сигнатур/docstring (защита от раздувания на больших модулях).
- **`grep_text`/`grep_code`: `regex` стал опциональным** (можно искать через алиас `query`); параметры grep-tools проброшены через federation в lockstep.

### Совместимость

- **Схема BSL-индекса: добавлены колонки `*_key`** (`data_links.to_object_key`, `role_rights.object_name_key`) с `DEFAULT ''` + индексы — аддитивно, существующие запросы не ломаются. На старых индексах ключи дозаполняются backfill'ом при первом старте нового бинарника.
- **Миграция существующих БД (`migrate_schema`-хук `LanguageProcessor`).** Перед `apply_schema_extensions` процессор языка идемпотентно догоняет недостающие `*_key`-колонки через `ALTER TABLE ADD COLUMN` (на свежей БД — no-op). Без этого апгрейд поверх БД от 0.20.0/0.21.0 ломался: `CREATE TABLE IF NOT EXISTS` не добавляет колонку в существующую таблицу, а `CREATE INDEX` по отсутствующей колонке рвал весь DDL-батч, из-за чего таблицы `role_rights`/`metadata_code_usages` не создавались, а `find_references` не работал.
- `lower()`/`upper()`-override меняет поведение только для кириллицы (латиница — как прежде); внутренние запросы и FTS не затронуты.
- Workspace version 0.21.0 → **0.22.0** (minor — новая функциональность + исправление).

## [0.21.0] — 2026-06-07

**Расширение графа данных 1С и «карта влияния» объекта (реверс связей + использование в коде + права ролей) + перевод хранения текстовых файлов на сжатый формат (`migrate_v5`).**

### Добавлено

- **Новые виды рёбер уровня конфигурации в `data_links`.** Парсер `bsl-extension/src/xml/metadata_refs.rs` (event-based `quick_xml`) добавляет к объектным рёбрам (`attr`/`tabular_attr`/`register_dim`/`recorder`/`owner`) четыре связи: `subsystem_content` (`from=Subsystem.X` — состав подсистемы), `exchange_plan_content` (`from=ExchangePlan.X` — состав плана обмена), `defined_type_content` (`from=DefinedType.X` — цели определяемого типа, через переиспользование `object_attributes::classify_type`), `functional_option_location` (`from=FunctionalOption.X`, `from_path` = полный `Location`). На боевой УТ: subsystem_content 22762, exchange_plan_content 9302, defined_type_content 4728, functional_option_location 564.
- **Таблица `role_rights`** (`repo, role_name, object_name, right_name`, UNIQUE + индексы по объекту и роли) — права ролей на объекты из `Roles/<X>/Ext/Rights.xml` (только `<value>true</value>`). Право — атрибут пары роль↔объект, поэтому отдельная таблица, а не ребро `data_links`. УТ: 49 695 прав / 1236 ролей / 6329 объектов.
- **Таблица `metadata_code_usages`** — обратный индекс использования объектов метаданных В КОДЕ `.bsl` (модуль `bsl-extension/src/code_usages.rs`, ручной сканер без `regex`): `manager` (`Документы.X` в коде), `ref_type` (`"ДокументСсылка.X"` / `"DocumentRef.X"` в литерале), `query` (путь метаданных в тексте запроса; 3-й сегмент → `member_path`). УТ: ~280 тыс. обращений (query 149 835 / manager 110 194 / ref_type 20 420). `object_ref_key` хранится в нижнем регистре (SQLite `lower()` не лоуэркейсит кириллицу) — для точечного поиска фильтровать по точному `object_ref`.
- **MCP-tool `find_references`** — «карта влияния» объекта одним вызовом: реверс `data_links` (по `to_object`) + `metadata_code_usages` + `role_rights`, с разбивкой по видам и примерами (`limit`).
- **MCP-tool `bsl_sql`** — произвольный read-only `SELECT`/`WITH` по `index.db` репо (длинный хвост запросов по метаданным/графам без отдельного named-tool). Guard: только `SELECT`/`WITH` + `Statement::readonly()` + потолок строк + таймаут.
- **MCP-tool `get_object_profile`** — полный «паспорт» объекта за один вызов (структура + формы + модули + связи данных) вместо серии `get_object_structure`/`get_form_handlers`/`get_data_links`.
- Число BSL-tools 8 → **11**. Все новые таблицы поддерживаются инкрементально в watcher-цикле демона (пересбор по компонентам пути / per-`.bsl`).

### Исправлено

- **Мердж `attributes_json` при наличии расширений** (`object_attributes::ObjectStructure::merge_from`) — реквизиты из базовой конфигурации и расширений объединяются, а не затирают друг друга. Рёбра `extension_override` в графе вызовов учитываются при пере/построении.

### Совместимость

- **Схема индекса v4 → v5.** Хранение текстовых файлов (md/xml/yaml/json/toml/sh…) переведено со схемы `text_files(content TEXT)` + external-content FTS на `text_contents(content_blob BLOB)` (zstd) + `fts_text_files` contentless, наполняемый из Rust. `migrate_v5` мигрирует существующие индексы НА МЕСТЕ при первом старте нового бинарника; свежие БД создаются сразу в новой схеме. `search_text`/`grep_text`/`read_file(text)`/`stat_file` работают как прежде. (Реализация — отдельный коммит `feat(core)`.)
- **Внешние потребители, читающие текст индекса напрямую через таблицу `text_files`, должны перейти на `text_contents`** (raw → zstd-decode). В частности `code-index-guard` (`serveability`) обновлён в lockstep: иначе на мигрированной БД запрос падает с `no such table: text_files`, и блокировка нативного `Read` молча отключается.
- Аддитивно для BSL: новые таблицы / виды рёбер / tools не ломают существующие ответы.
- Workspace version 0.20.0 → **0.21.0** (minor — новая функциональность).

## [0.20.0] — 2026-06-06

**`_meta.file_mtimes` в ответах поисковых tools + ранний сигнал демона `POST /mark-dirty` — для write-triggered ленивой ревалидации кэша `mcp-cache-ci` (#1471).**

### Добавлено

- **`_meta.file_mtimes` в ответах MCP-tools.** Рядом с существующим `_meta.dependent_files` serve теперь отдаёт карту `{<rel_path>: <index_mtime>}` (unix-секунды из колонки `files.mtime`) по каждому зависимому файлу. Это вход для write-triggered ленивой ревалидации в `mcp-cache-ci`: прокси сверяет индексный mtime с observed-mtime из `mark-dirty` и кэширует ответ только когда индекс догнал диск (`index_mtime >= observed`). Реализовано в `wrap_with_meta` (батч через новый `Storage::mtime_for_path`), применяется ко всем cacheable поисковым tools (`search_function`/`get_function`/`grep_body`/`grep_code`/`read_file`/`get_file_summary`/...). `stat_file` не затронут (non-cacheable, `_meta` не несёт).
- **Демон шлёт `POST /mark-dirty` при FS-событии.** В начале обработки батча (ДО переразбора/commit), в дополнение к `POST /invalidate` после commit, демон отправляет cache-ci `{repo, files:[{path, mtime}]}` с observed-mtime изменённых файлов (`PathEntry::effective_alias()` как `repo`). Прокси сразу помечает зависимые записи грязными, сужая окно, в котором кэш мог бы отдать старьё; флаг снимается сверкой mtime на стороне cache-ci. Best-effort: ошибки и 404 (cache-ci без поддержки) логируются, демон не блокируется. Новый `CacheClient::mark_dirty_files`, helper `collect_dirty_paths` в `worker.rs`.

### Совместимость

- **Аддитивно, не breaking.** `_meta.file_mtimes` — новое поле рядом с `dependent_files`, старые потребители его игнорируют. `mark-dirty` — отдельный канал в дополнение к `invalidate`. Полный эффект — с `mcp-cache-ci` ≥ 0.4.0; со старым cache-ci `mark-dirty` вернёт 404 (проглатывается), `file_mtimes` игнорируется.
- BSL-tools (`bsl-extension`) пока **не** добавляют `file_mtimes` (follow-up): для dirty-файлов, от которых зависят только BSL-ответы, cache-ci продолжит форвардить, пока путь грязный (безопасная деградация).
- Workspace version 0.19.2 → **0.20.0** (minor — новая функциональность).

## [0.19.2] — 2026-06-06

**Переименование файла в новое имя больше не оставляет в индексе осиротевшую строку под старым именем.**

### Исправлено

- **Watcher корректно удаляет старое имя при переименовании файла.** Раньше `notify`-событие `Modify(Name(RenameMode::From))`, приходящее на уже исчезнувший путь старого имени, либо отбрасывалось проверкой `!path.is_file()`, либо превращалось в `Modified` и тихо гасло по `NotFound` при хешировании — строка старого имени оставалась фантомом в индексе до следующего полного reindex (попадала в `stat_file`/`list_files`/граф с устаревшими данными). Логика классификации вынесена в `classify_event`: каталоги игнорируются, а `Create`/`Modify` на отсутствующем на диске пути трактуются как `Deleted`. Покрыто тестом `test_classify_event_rename_from_becomes_delete`. (Atomic-save `tmp`→rename поверх существующего файла работал и раньше и продолжает работать — целевой путь остаётся файлом.)

## [0.19.1] — 2026-06-06

**Инкрементальный путь демона теперь пишет `mtime`/`file_size` для новых и изменённых файлов — раньше watcher оставлял эти поля NULL.**

### Исправлено

- **`Storage::upsert_file` записывает `mtime`/`file_size`** (добавлены в `INSERT` и `ON CONFLICT DO UPDATE` с `COALESCE`, чтобы не затирать реальные значения `NULL`-ом). При живом демоне создание/изменение файла раньше оставляло строку `files` с `mtime = NULL` и `file_size = NULL`: значения честно доезжали в `FileRecord` (и из полной индексации, и из watcher'а через `std::fs::metadata`), но `upsert_file` их игнорировал. Реальный `mtime` писался только отдельным `update_file_metadata` — а он на пути записи вызывается лишь для файлов с неизменным хешем, поэтому свежесозданные/только что изменённые файлы (самые «горячие») оставались с пустым `mtime`. Бьёт по `stat_file`/`list_files`, по дешёвой проверке свежести (`code-index-guard`) и по фазе 1b «быстрый skip по mtime+size» при следующих полных reindex. Теперь оба пути пишут `mtime`/`file_size` одной точкой. Покрыто регрессионным тестом `test_upsert_file_persists_mtime_and_size`.

## [0.19.0] — 2026-06-05

**Онлайн-обновление extras-слоя при live-редактировании: после правки файла граф вызовов, связи данных и структура объектов обновляются инкрементально прямо в watcher-цикле демона — точечно (стоимость ~ от размера изменённого файла, не графа), без рестарта и без полного XML-обхода.**

Раньше в демоне extras-слой (`proc_call_graph`, `data_links`, `metadata_objects.attributes_json`, `metadata_forms`, `event_subscriptions`) строился один раз при старте worker'а и протухал до перезапуска: `find_path`/`get_callers`/`get_data_links`/`get_object_structure` отдавали устаревшую картину во время сессии редактирования. Полный пересбор (`run_index_extras`) дорогой (~2 с, обходит все XML), поэтому на каждое сохранение не вызывался.

### Добавлено

- **Инкрементальное обновление extras в watcher-цикле демона** (после `commit_batch`), с маршрутизацией по типу изменённого файла:
  - `.bsl` → **per-file** обновление слоя `direct` графа вызовов: трогаются только рёбра изменённого файла (прежние — из side-карты `direct_edge_files`, текущие — из core-таблицы `calls`), стоимость не зависит от размера графа. На КА 1.1 (~123k рёбер) — ~0 мс против ~2 с при пересборе всего слоя;
  - объектный XML (Catalogs/Documents/Registers/…) → per-object обновление `data_links` (по `from_object`) и структуры объекта (`attributes_json`);
  - `Form.xml` / `EventSubscriptions/*.xml` → per-file обновление строки + slice-rebuild соответствующего слоя графа (`form_event` / `subscription`);
  - `Configuration.xml` (изменение состава объектов) → полный `run_index_extras`.
- Новый метод `LanguageProcessor::index_extras_for_files` (реализация по умолчанию — no-op; универсальные языки не затрагиваются). BSL реализует через `run_incremental_extras`.

Для per-file обновления графа добавлена служебная таблица `direct_edge_files` (привязка direct-рёбер к файлам-источникам); `proc_call_graph` остаётся дедуплицированной, поэтому `find_path`/`find_data_path` не затронуты. В лог worker'а добавлен тайминг обновления extras (полный и инкрементальный пути). Эквивалентность инкремента полному пересбору закреплена тестами (`incremental_object_xml_matches_full`, `incremental_call_graph_direct_matches_full`, `incremental_direct_shared_edge_survives`).

## [0.18.0] — 2026-06-05

**Точечные доработки BSL-инструментов и CLI по итогам E2E-сравнения с `rlm-tools-bsl` (КА 1.1): фильтр подписок по короткому имени модуля, подсказка `search_terms` при пустом обогащении, быстрый `index --force` и PID-lock на разовую индексацию, актуализированное описание `get_object_structure`.**

### Добавлено

- **`search_terms` отдаёт `hint` при пустом результате и пустой таблице обогащения** — сообщает, что `bsl-indexer enrich` не запускался для репо, и направляет на `grep_body`/`grep_code`/`search_function`/`get_function`. Раньше пустой ответ читался как «нет совпадений», и вызов тратился впустую. (E1)
- **PID-lock на команду `index`** — два параллельных `index` по одному пути больше не дерутся за SQLite (RAII-lock на `index.lock` рядом с `index.db`). Общий механизм с демоном (`acquire_at`). (A2)

### Изменено

- **`get_event_subscriptions`: фильтр `handler_module` матчит и полное имя (`CommonModule.X`), и короткое (`X`)** через суффиксный `LIKE '%.X'`. Раньше короткое имя не находило подписку. (D1)
- **`index --force` пересоздаёт `index.db` с нуля** вместо upsert поверх существующей БД. На большой БД старый путь был патологически медленным (полная загрузка в RAM + upsert каждого файла); удаление файла превращает `--force` в быстрый fresh-путь с тем же итогом. (A1)
- **Актуализировано описание `get_object_structure`** — отражает полную структуру (реквизиты с типами, ТЧ, измерения/ресурсы, `enum_values`, `predefined`, всегда-присутствующие базовые секции) и явно указывает, что XML объектов не индексируется как текст (не искать через `list_files`/`grep_text`). (D2)

## [0.17.0] — 2026-06-05

**`get_object_structure`: секция `predefined` — имена предопределённых элементов (Catalog / ChartOfAccounts / ChartOf*).**

Закрывает пробел C2 из E2E-сравнения с `rlm-tools-bsl`: предопределённые элементы (`Справочники.Качество.Новый` и т.п.) лежат в отдельном `<Объект>/Ext/Predefined.xml` и раньше требовали ручного чтения XML. Теперь — секция `predefined` прямо в структуре объекта.

### Добавлено

- **`get_object_structure` отдаёт секцию `predefined`** — имена предопределённых элементов объекта из соседнего `<Объект>/Ext/Predefined.xml` (`<Item>/<Name>`). Заполняется при индексации для `Catalog`/`ChartOfAccounts`/`ChartOfCharacteristicTypes`/`ChartOfCalculationTypes`; у объектов без предопределённых секция отсутствует. Проверка на живой КА 1.1: `Catalog.Качество` → `["Новый"]`, `Catalog.СтатьиЗатрат` → `["СписаниеНДСНаРасходы","СписаниеНДСНаРасходыПрочие"]`.

## [0.16.0] — 2026-06-05

**1С-инструменты метаданных: `get_object_structure` отдаёт полную структуру (включая значения перечислений), новый инструмент `get_register_writers` (регистраторы регистра / движения документа), имена событий подписок нормализованы к русским.**

Серия доработок BSL-слоя по итогам E2E-сравнения с `rlm-tools-bsl` на конфигурации КА 1.1: закрыт главный пробел «какие документы пишут движения в регистр», `get_object_structure` перестал быть заглушкой для перечислений и всегда отдаёт предсказуемую форму ответа.

### Добавлено

- **Новый MCP-инструмент `get_register_writers`** — регистраторы регистра и движения документа по составу `<RegisterRecords>` (recorder-рёбра графа `data_links`). Для регистра (`AccumulationRegister.ТоварыНаСкладах`) возвращает в `writers` список документов, пишущих движения; для документа (`Document.РеализацияТоваровУслуг`) — в `writes_to` список регистров-приёмников. Один вызов закрывает оба направления, тип объекта определять заранее не нужно. Итого 8 BSL-инструментов поверх 18 универсальных.
- **recorder-рёбра в графе `data_links`** — связь «документ → регистр» (`link_kind=recorder`) из декларативного состава движений документа. `get_data_links(register, direction=in)` теперь показывает документы-регистраторы (ранее — пусто, движения регистров граф не моделировал). Источник — `<RegisterRecords>` XML документа, а не разбор кода проведения — без ложных срабатываний.
- **`get_object_structure` для перечислений** — секция `enum_values` со значениями перечисления (ранее для `Enum.*` возвращалась пустая структура). Папка `Enums` добавлена в обход индексатора метаданных.

### Изменено

- **`get_object_structure` отдаёт полную структуру объекта** — реквизиты с типами в 1С-нотации (`СправочникСсылка.X`, составные через `|`), табличные части с колонками, измерения/ресурсы регистров. Ранее — documented-заглушка `attributes: null`.
- **`get_object_structure` всегда эмитит базовые секции** `attributes`/`dimensions`/`resources`/`tabular_sections` (пустые — как `[]`, а не опускаются). Потребитель отличает «секции нет» от «инструмент её не вернул» и не уходит в сырой XML.
- **Имена событий в `get_event_subscriptions` нормализованы к русским** (`OnWrite`→`ПриЗаписи`, `Posting`→`ОбработкаПроведения` и т.д.); фильтр двусторонний — принимает и русское имя, и английский enum платформы.

### Исправлено

- **Актуализирован docstring инструмента `read_file`** — он отдаёт content и для code-файлов (zstd-decode из `file_contents`, Phase 2 v0.8.0+; поле `category` = `"text"`/`"code"`), а не «только text-файлы, для code пусто». Прежнее описание устарело после Phase 2 и вводило в заблуждение.

## [0.15.0] — 2026-06-04

**`grep_text` и `grep_body`: вывод сгруппирован по файлу + флаг `truncated` — устранено дублирование пути, тот же приём, что был сделан для `grep_code` в 0.14.0.**

`grep_text` и `grep_body` отдавали плоский массив находок, где полный путь к файлу повторялся в каждой находке. При множестве совпадений в одном файле это раздувало ответ (и оплачиваемые токены при работе через API). `grep_code` ещё в 0.14.0 перешёл на группировку `{files: {"<path>": [...]}}`; теперь тот же приём применён к двум оставшимся grep-инструментам.

### Изменено

- **`grep_text` группирует находки по файлу.** Формат ответа `[{path, line, content, context}]` → `{files: {"<path>": [{line, content, context?}]}, shown, limit, truncated}`. Путь — один раз как ключ в `files`; `context` опускается при `context_lines=0`.
- **`grep_body` группирует находки по файлу.** Формат `[{file_path, name, kind, …}]` → `{files: {"<path>": [{name, kind, line_start, line_end, match_lines, match_count?, context?}]}, shown, limit, truncated}`. `match_count` опускается, когда совпадений ≤3; `context` — при `context_lines=0`.
- **Оба инструмента возвращают `truncated`.** Storage-методы `grep_text_filtered` и `grep_body_with_options` теперь возвращают `(Vec, bool)` — флаг выставляется при достижении `limit` или байтового потолка ответа (1 МБ), как у `grep_code`. Для легаси-пути `grep_body` (без `path_glob`/`context_lines`) `truncated` выводится из достижения `limit`. Модель видит, что показано не всё, и при необходимости дошлёт больший `limit`.

### Совместимость

- Потребители, парсившие плоский массив `grep_text`/`grep_body`, должны читать `result.files` (объект «путь → массив находок») вместо массива; поле пути ушло из каждой находки в ключ. Разовый breaking-change формата вывода — аналогично `grep_code` в 0.14.0.

## [0.14.2] — 2026-05-31

**`find_data_path`: обход переписан на BFS с visited-set — устранён комбинаторный взрыв на плотном графе связей.**

После 0.14.1 (ANALYZE починил seek) обход `find_data_path` на плотном циклическом графе связей данных всё ещё мог разворачивать миллионы путей: рекурсивный CTE перечислял ВСЕ пути до max_depth без дедупликации узлов (на КА 1.1 плотный узел при depth=4 — ~4.9 млн промежуточных строк плюс рост памяти на `path_json`).

### Исправлено

- **`find_data_path` использует BFS с visited-set вместо рекурсивного CTE.** Каждый объект разворачивается ровно один раз (HashSet посещённых) → обход ограничен достижимым подграфом (тысячи узлов), а не числом путей (миллионы); циклы графа связей больше не гоняются по кругу. Возвращается тот же кратчайший по числу рёбер путь from → to. Терминальные обобщённые `*`-узлы исходящих рёбер не имеют и не разворачиваются. На каждом шаге — индексный seek по `(repo, from_object)` (обеспечен ANALYZE из 0.14.1). `find_path` (граф вызовов) не затронут — там CTE остаётся, ANALYZE его уже сделал быстрым.

## [0.14.1] — 2026-05-31

**`find_path`/`find_data_path`: устранены таймауты обхода графов на больших BSL-репо (`ANALYZE` после построения графов).**

На крупных конфигурациях (КА 1.1 — `proc_call_graph` ~125k рёбер, `data_links` ~11.5k) обход `find_path`/`find_data_path` упирался в таймаут: depth=3 у графа вызовов считался ~240с. Причина — в SQLite-индексе репо отсутствовала статистика (`sqlite_stat1`), и планировщик в рекурсивном шаге CTE брал только константный префикс индекса (`repo=`), сканируя все рёбра репо на каждой итерации вместо seek по `(repo, caller_proc_key)` / `(repo, from_object)`. Форсирование индекса через `INDEXED BY` не помогало — рычагом является только статистика.

### Исправлено

- **Индексатор гонит `ANALYZE` в конце `run_index_extras`** — после перестройки `proc_call_graph`/`data_links` (и прочих BSL-таблиц) собирается статистика SQLite, и планировщик начинает использовать seek по двум столбцам в рекурсивном шаге `find_path`/`find_data_path`. На КА 1.1: `find_path` depth=3 240с → 0.05с. Стоимость `ANALYZE` — ~0.6с на БД 2.4 ГБ (сканируются B-деревья индексов, не zstd-блобы content); выполняется при каждом (пере)индексе синхронно с перестройкой графов.

### Известные ограничения

- `find_data_path` на **плотном узле при глубине 4** даже после `ANALYZE` обходит миллионы путей — в рекурсивном CTE нет visited-set/cycle-detection, а граф связей данных 1С густой и циклический. Для типичных запросов и глубины ≤3 — мгновенно; устранение взрыва на глубоких плотных обходах запланировано отдельно.

## [0.14.0] — 2026-05-30

**`grep_code`: находки сгруппированы по файлу — путь не повторяется в каждой строке.**

Раньше `grep_code` отдавал плоский массив, где полный путь файла дублировался в каждой находке — а находки часто кучкуются в одном файле (десятки совпадений с одинаковым `path`). Теперь находки группируются: путь — ключ объекта `files`, под ним список `{line, content, context}`. На кучных результатах это заметно сокращает ответ.

### Изменено

- **Формат результата `grep_code`**: `{matches: [{path, line, content, context}], …}` → `{files: {"<path>": [{line, content, context}], …}, shown, limit, truncated}`. Путь хранится один раз на файл. Поле `context` опускается при `context_lines=0`. Поля `shown`/`limit`/`truncated` — без изменений.

### Совместимость

- **Изменение формата ответа `grep_code`** (массив `matches` → объект `files`, сгруппированный по пути). Потребители читают `result.files["<path>"]` вместо `result.matches[].path`. `grep_text`/`grep_body` не затронуты — там формат прежний.

## [0.13.0] — 2026-05-30

**Compact-JSON в выводе MCP-инструментов вместо pretty.**

Вывод MCP-инструментов читает модель, а не человек — отступы и переносы pretty-JSON тратят токены без пользы. Переводим сериализацию ответов на compact (`to_string` вместо `to_string_pretty`). Экономия ~30% на каждом ответе tool'а, особенно заметна для federation (удалённые репо) и для текстовых инструментов. Смысл данных не меняется — JSON тот же, просто без форматирования.

### Изменено

- **Сериализация ответов MCP-инструментов — compact** (`to_string`): `wrap_with_meta` (18 универсальных tools — read_file, grep_*, get_function, list_files и т.д.), `to_json` (`get_stats`/`stat_file`/`health`), `format_unavailable`, federation-форвард (`federation_error` + агрегация `get_stats` по репо).
- BSL-tools уже отдавали compact через `CallToolResult::structured` — не затронуты.

### Совместимость

- Формат данных не изменился — убрано только pretty-форматирование (отступы/переносы). Любой JSON-парсер читает результат как раньше.
- **CLI-вывод** (`--json`) и файлы `daemon.json`/`config.json` остаются pretty — они человекочитаемы и не на горячем пути модели.

## [0.12.0] — 2026-05-30

**`grep_code`: дефолт `limit` снижен 500→100 и добавлен явный флаг обрезки `truncated`.**

По статистике реального использования (выборка за 2 месяца, ~240 вызовов `grep_code`): когда модель сама задаёт `limit`, она ставит ~20–40 совпадений (медиана 30), а значение 500 указала лишь дважды из сотни вызовов. Прежний дефолт 500 (при заданном `path_glob`/`language`) раздувал ответ вдвое против нативного Grep (`head_limit` 250) — особенно с `context_lines`. Снижаем дефолт до 100 и делаем обрезку видимой, чтобы модель при необходимости дослала больший `limit`, а не считала обрезанный список полным.

### Изменено

- **`grep_code` дефолт `limit` 500 → 100** (новая константа `GREP_CODE_DEFAULT_LIMIT`). Раньше дефолт зависел от фильтра: 100 при full-scan / 500 при заданном `path_glob`/`language`; теперь единый дефолт 100. Явно переданный `limit` работает как прежде.
- **Формат результата `grep_code`**: вместо голого массива `[{path, line, content, context}]` теперь объект `{matches: [...], shown, limit, truncated}`. Поле `truncated=true` означает, что результат обрезан по `limit` или по байтовому потолку (1 МБ) — есть ещё совпадения, дошлите больший `limit`. Раньше обрезка была тихой и читалась как «это все совпадения».
- **`Storage::grep_code_filtered`** теперь возвращает `(Vec<GrepTextMatch>, bool)` — второй элемент кортежа — признак обрезки.

### Совместимость

- **Изменение формата ответа `grep_code`** (массив → объект `{matches, …}`). Потребители, парсившие ответ как массив, должны читать `result.matches`. `mcp-cache-ci` (использует только `_meta.dependent_files`) и federation-форвардинг не затронуты. Формат `grep_text`/`grep_body` **не менялся** — там по-прежнему массив.

## [0.11.0] — 2026-05-30

**Опциональный whitelist MCP-инструментов через `[tools].enabled` в `daemon.toml`.**

Продолжаем борьбу за ваши токены и скорость: теперь сервер можно настроить так, чтобы он отдавал в `tools/list` только нужное подмножество tools, а не все 25 (18 универсальных + 7 BSL). Меньше schema-токенов в каждом `initialize`, меньше путаницы у слабых моделей при выборе tool, тот же функционал для сильных. Поведение по умолчанию не меняется — если секции `[tools]` нет или `enabled` пуст, все зарегистрированные tools остаются доступны (обратная совместимость).

### Добавлено

- **Секция `[tools]` в `daemon.toml`** с полем `enabled: Vec<String>`. Пустой массив или отсутствие секции — все tools доступны. Заполненный — только перечисленные имена попадают в `tools/list`, остальные блокируются на `tools/call` с ошибкой `-32602 Invalid params: tool 'X' is disabled by [tools].enabled whitelist in daemon.toml`. Двойная защита нужна потому, что модель может вызвать tool из своей памяти / системного промпта, минуя `tools/list` — фильтр только в `list_tools` тут бы не помог.
- **`CodeIndexServer::with_allowed_tools(Option<BTreeSet<String>>)`** — builder для установки whitelist'а программно (используется в `cli.rs`).
- **`CodeIndexServer::validate_whitelist(&BTreeSet<String>) -> Vec<String>`** — возвращает имена tools, которых нет среди зарегистрированных (опечатки, удалённые tools). Используется в `cli.rs` для warning при старте.
- **Логи при старте сервера**: при пустом `enabled` — `[tools].enabled пуст — whitelist выключен, все tools доступны`; при непустом — `[tools].enabled whitelist активен: N известных tools разрешены (M в списке)` + warning при опечатках.
- **3 теста парсинга секции `[tools]`** в `daemon_core::config::tests` (`tools_section_default_empty`, `parses_tools_whitelist`, `parses_empty_tools_section`).

### Совместимость

- Полностью обратно совместимо. Старые `daemon.toml` без секции `[tools]` продолжают работать как раньше (все tools доступны). Поведение по умолчанию — то же, что в v0.10.x.
- Минимальный функционально безопасный набор: `read_file`, `grep_code`, `get_function`, `find_symbol`, `list_files`, `get_stats`, `health`. Урезание ниже этого набора (например, оставить только `grep_body` без `grep_code`) приведёт к слепоте на импортах/директивах/module-level коде и обходам через дорогой полный `read_file` — экономия токенов будет уничтожена.

## [0.10.4] — 2026-05-22

**Фикс публикации в MCP-реестр: регистр namespace.**

Реестр отклонял `server.json` с 403 — namespace был указан строчными (`io.github.regsorm`), а OIDC выдаёт права на namespace, точно совпадающий с GitHub-логином (`io.github.Regsorm`). Исправлен регистр в `name`/`mcpName`. npm-публикация в 0.10.2/0.10.3 уже прошла; этот патч доводит регистрацию визитки в официальном реестре.

### Исправлено

- **`server.json` `name` и `package.json` `mcpName`** — регистр namespace `io.github.Regsorm/code-index` (точно как GitHub-логин).

### Изменено

- **Workspace version** 0.10.3 → 0.10.4.

## [0.10.3] — 2026-05-22

**Фикс публикации в MCP-реестр: длина описания.**

Реестр отклонял `server.json` с 422 — поле `description` превышало лимит 100 символов. Укорочено до 98. npm-публикация в 0.10.2 уже прошла успешно; этот патч доводит регистрацию визитки в официальном реестре.

### Исправлено

- **`server.json` `description`** укорочено до ≤100 символов (требование реестра).

### Изменено

- **Workspace version** 0.10.2 → 0.10.3.

## [0.10.2] — 2026-05-22

**Фикс автопубликации: рабочий триггер workflow.**

В 0.10.1 публикация не сработала — `publish-registry.yml` был на триггере `workflow_run`, который GitHub запускает только при наличии файла в default-ветке (`main`); релизы же тегируются из рабочей ветки. Плюс паттерн скачивания `mcp-publisher` цеплял лишний ассет.

### Исправлено

- **Триггер `publish-registry.yml`** переведён с `workflow_run` на `push: tags: ['v*']` — работает из любой ветки. Добавлен шаг ожидания готовности GitHub Release (архивов code-index) перед `npm publish`, чтобы исключить гонку.
- **Скачивание `mcp-publisher`** — точный паттерн ассета `mcp-publisher_linux_amd64.tar.gz` (раньше `*linux_amd64.tar.gz` цеплял и `registry_linux_amd64.tar.gz`).
- Команды `mcp-publisher login github-oidc` и `publish` сверены с реальным CLI (v1.7.9).

### Изменено

- **Workspace version** 0.10.1 → 0.10.2.

## [0.10.1] — 2026-05-22

**Публикация в npm и официальный MCP-реестр.**

Публичный `code-index` теперь ставится через `npx`/`npm` и регистрируется в [официальном MCP-реестре](https://registry.modelcontextprotocol.io/) (`io.github.regsorm/code-index`). Бинарник на Rust по-прежнему распространяется через GitHub Releases — npm-пакет лишь тонкая обёртка, которая на установке скачивает архив под текущую платформу. `bsl-indexer` остаётся приватным, в реестр не публикуется.

### Добавлено

- **npm-обёртка `@regsorm/code-index-mcp`** (каталог `npm/`): `package.json` с `mcpName`, `bin/cli.js` (прозрачно проксирует аргументы и stdio в нативный бинарник), `scripts/postinstall.js` (скачивает архив `code-index-<platform>` из GitHub Releases и распаковывает системным `tar`/bsdtar). Поддержка Windows x64, Linux x64, macOS arm64.
- **`server.json`** — визитка для официального MCP-реестра (npm-пакет, транспорт stdio, подкоманда `serve`).
- **`.github/workflows/publish-registry.yml`** — после успешного `Release` на тег `v*`: `npm publish` + `mcp-publisher publish`. Версия подставляется из тега. Требует секрет `NPM_TOKEN`.

### Изменено

- **Workspace version** 0.10.0 → 0.10.1.

### Совместимость

- Полностью обратно совместимо. Изменений в коде индексатора нет — только инфраструктура распространения.

## [0.10.0] — 2026-05-21

**Граф связей данных 1С (data-graph): новые BSL-tools `get_data_links` и `find_data_path`.**

Дополняет граф ВЫЗОВОВ (`proc_call_graph`) графом СВЯЗЕЙ ДАННЫХ — рёбра «объект → объект» по ссылочным типам реквизитов, измерений регистров и реквизитов табличных частей. Закрывает частый паттерн «блуждания по структуре»: вместо серии `get_object_structure`/`get_metadata_structure` для трассировки связей вручную — один обход графа. (На реальном кейсе «схлопнуть остатки по ГТД» это было 37 запросов структуры → теперь один `get_data_links`.)

### Добавлено

- **Таблица `data_links`** в `bsl-extension` schema (`SCHEMA_EXTENSIONS`, аддитивно через `CREATE TABLE IF NOT EXISTS` — миграция не требуется): `from_object`, `from_path` (реквизит / `ТЧ.реквизит` / измерение), `to_object`, `link_kind` (`attr`/`tabular_attr`/`register_dim`), `is_composite`, `is_universal`. Индексы `idx_dl_from` (прямой обход) и `idx_dl_to` (обратный — «кто ссылается на X»).
- **`crates/bsl-extension/src/xml/object_attributes.rs`** — парсер ссылочных типов из XML отдельных объектов (`Catalogs/<X>.xml`, `Documents/<Y>.xml`, регистры). Классификация типа: конкретный `cfg:CatalogRef.Контрагенты` → ребро `Catalog.Контрагенты`; составной (несколько `<v8:Type>`) → несколько рёбер (`is_composite`); обобщённый (`cfg:CatalogRef` без имени, `cfg:AnyRef`, `cfg:DefinedType.X`) → терминальный `*`-узел (`is_universal`, не разворачивается при обходе — защита от fan-out и шума); примитивы (`xs:`/`v8:`) отбрасываются. Страховочный cap на патологические перечни (>30 конкретных типов → `*Multiple`).
- **`index_data_links`** в `index_extras::run_index_extras` — обход объектных XML и наполнение `data_links` полным пересбором (как остальной `index_extras`). На крупной конфигурации (~1900 объектных XML / ~68 МБ) — ~1.3–1.9 сек, инкрементальность не нужна.
- **MCP-tool `get_data_links(repo, object, direction=out|in|both, depth=1..4)`** — окрестность объекта в графе связей данных через recursive CTE. `out` — на что ссылается; `in` — кто ссылается; терминальные `*`-узлы при обходе не разворачиваются.
- **MCP-tool `find_data_path(repo, from, to, max_depth=4)`** — путь (цепочка ссылочных связей) между двумя объектами (BFS по `data_links`, аналог `find_path` для графа вызовов).
- Оба tool зарегистрированы в `BslLanguageProcessor::additional_tools` (теперь **7 BSL-tools**, итого **25** в сборке `bsl-indexer`), доступны и через federation (`POST /federate/extension`). Юнит-тесты парсера (3 случая типов, ТЧ, измерения, cap) и наполнения.

### Изменено

- **Workspace version** 0.9.1 → 0.10.0.

### Совместимость

- Полностью обратно совместимо. Новая таблица создаётся идемпотентно при старте; существующие индексы и tools не затронуты. Публичный бинарник `code-index` не меняется — фича только в `bsl-indexer` (`bsl-extension`).

## [0.9.1] — 2026-05-12

**Этап 3 миграции на event-based cache invalidation: уведомление `mcp-cache-ci` после переиндексации.**

Замыкает цепочку: сохранили файл → daemon (watcher) обнаружил → переиндексировал в SQLite → **отправил `POST /invalidate {file_paths: [...]}` в cache-ci**. Cache-ci по reverse_index (заполненному в этапе 2 через `_meta.dependent_files`) точечно сносит только зависимые entries, остальные cache hits сохраняются.

### Добавлено

- **`crates/code-index-core/src/daemon_core/cache_client.rs`** — `CacheClient` с пулом `reqwest::Client` (timeout 2s, keep-alive 60s) и списком target URL-ов. Метод `invalidate_files(&[String])` шлёт POST параллельно всем targets, на failure (сеть, 5xx, timeout) — `eprintln!` warning и продолжаем; падать не должны, TTL на стороне cache-ci подстрахует.
- **Секция `[[cache_targets]]` в `daemon.toml`** + структура `CacheTargetEntry { url: String }` в `daemon_core/config.rs`. Пример:

  ```toml
  [[cache_targets]]
  url = "http://127.0.0.1:8011"
  ```

  Несколько entries разрешено (multi-cache-ci топологии: локальный Windows + удалённый rag-VM). Отсутствие секции (или пустой список) → событийный канал выключен, поведение как до v0.9.1.
- **Хелпер `worker::collect_invalidate_paths(root, batch)`** — собирает дедуплицированный список relative file_path'ей из batch'а FS-событий. Учитывает все типы (Modified/Created/Deleted) — удаление файла тоже должно сносить связанные cache_entries.
- **Параметр `cache_client: Option<Arc<CacheClient>>`** в `worker::run_worker` и `runner::spawn_worker`. Пробрасывается из `runner::run` и `runner::handle_reload` (reload пересоздаёт `CacheClient` по новому конфигу для added-папок; existing workers сохраняют свой client до рестарта demon).
- **Юнит-тесты** для `cache_client.rs`: пустые targets → `is_empty()`; trailing slashes стрипуются; невалидный target не паникует (connection refused → 0 успехов). Тесты для config.rs `cache_targets_default_empty` и `parses_cache_targets_list`.

### Изменено

- **Сигнатура `worker::run_worker`** — новый последний параметр `cache_client`.
- **Сигнатура `runner::spawn_worker`** — то же.
- **`commit_batch()` теперь возвращает результат проверки** — если commit упал, invalidate не отправляется (новых данных в индексе всё равно нет; cache-ci пусть отдаёт старое — будет corrected либо при следующем успешном batch'е, либо через TTL).
- **Workspace version** 0.9.0 → 0.9.1.

### Совместимость

- `daemon.toml` без `[[cache_targets]]` — полностью работающий (поведение как до v0.9.1, без сетевого трафика к cache-ci).
- `daemon.toml` с `[[cache_targets]]` — событийный канал активируется автоматически на старте.
- API `run_worker` / `spawn_worker` — изменилась сигнатура (additive last param). Внешние клиенты крейта `code-index-core` (если есть) должны передать `None` для совместимости.

### Архитектура (final state цепочки)

После v0.9.1 + cache-ci 0.2.0:

1. **Read-tools daemon'а** возвращают `{result, _meta: {dependent_files: [...]}}` (v0.9.0).
2. **`mcp-cache-ci`** при cache-fill пишет `cache_key → file_paths` в reverse_index (cache-ci 0.2.0).
3. **Daemon watcher** на FS event → reindex → `commit_batch` → `cache_client.invalidate_files(...)` → cache-ci по reverse_index сносит точечно (v0.9.1).
4. **TTL fallback** — третий эшелон safety net: если событие потерялось (сеть, daemon упал, ReadDirectoryChangesW buffer overflow), entry протухнет за 600s/3600s сам.

## [0.9.0] — 2026-05-12

**Phase 2 (этап миграции на event-based cache invalidation): `_meta.dependent_files` в read-ответах.**

Все data-инструменты MCP теперь возвращают единый JSON-формат:

```json
{
  "result": <prev plain payload>,
  "_meta": { "dependent_files": ["src/X.bsl", "src/Y.bsl"] }
}
```

`dependent_files` — список path'ей файлов, из которых построен этот ответ. Целевой потребитель — `mcp-cache-ci`: при cache-fill он регистрирует связи `cache_key → file_path` в `reverse_index` и затем сносит точечно затронутые entries по сигналу от daemon после переиндексации файла (этап 3, готовится).

### Совместимость (BREAKING CHANGE формата ответа)

Все клиенты read-tools должны быть готовы к новой структуре `{result, _meta}`:

- Раньше: `search_function` возвращал плоский массив `[FunctionRecord, ...]`.
- Сейчас: `{"result": [FunctionRecord, ...], "_meta": {"dependent_files": [...]}}`.

Для существующего потребителя (`mcp-cache-ci` 0.2.0+) поведение обратно-совместимое: cache-ci парсит `_meta.dependent_files` если есть, иначе работает как раньше (insert без зависимостей, TTL fallback).

Tools **без** обёртки (формат ответа не изменён):

- `health` — non-cacheable.
- `get_stats` — диагностический, формат расширяется по федерации, обёртка ломала бы агрегацию.
- `stat_file` — single-file тривиальный.

### Добавлено

- **Wrapper-хелперы в `crates/code-index-core/src/mcp/tools.rs`:**
  - `wrap_with_meta<T: Serialize>(result, dependent_files)` — финальная сериализация в `{result, _meta}` с дедупликацией file_paths.
  - `collect_paths_via<R>(storage, records, extract: fn(&R) -> file_id)` — собрать пути из vec'а records через extractor.
- **Wrapper-хелперы в `crates/bsl-extension/src/tools/mod.rs`:**
  - `wrap_with_meta(result: Value, dependent_files: Vec<String>) -> Value` для BSL extension-tools.
  - `wrap_error(error_value: Value) -> Value` — даже на ошибке формат единый.
- **Поддержка `_meta.dependent_files` в data-tools core:**
  - `search_function`, `search_class` — DISTINCT file_paths из Vec'а records.
  - `get_function`, `get_class` — то же.
  - `find_symbol` — объединение path'ей из functions+classes+variables+imports.
  - `get_imports` (by file и by module).
  - `get_file_summary` — path из args.
  - `get_callers`, `get_callees` — file_ids из CallRecord.
  - `grep_body` — file_path напрямую из GrepBodyMatch.
  - `grep_code`, `grep_text`, `search_text` — path напрямую из match-структур.
  - `read_file` — path из args.
  - `list_files` — paths из ListedFile.
- **Поддержка `_meta.dependent_files` в BSL extension-tools** (пока пустой массив — XML-парсер метаданных не привязан к file_path, реальные зависимости — задача следующей итерации):
  - `get_object_structure`, `get_form_handlers`, `get_event_subscriptions`, `find_path`, `search_terms`.

### Изменено

- **Workspace version** bumped 0.8.1 → 0.9.0 (minor — обратно-совместимое расширение формата для cache-ci-клиента, breaking для клиентов, парсивших плоский payload).

### Следующие шаги

- Этап 3: `POST /invalidate {file_paths}` от daemon к cache-ci после `transaction.commit()` SQLite по batch'у FS-событий. На стороне cache-ci 0.2.0 уже готово принимать.

## [0.8.1] — 2026-05-06

**Patch-релиз: BSL extension-tools в daemon-режиме и через federation.** Закрывает две публичные регрессии v0.8.0, из-за которых пять BSL-инструментов (`get_object_structure`, `get_form_handlers`, `get_event_subscriptions`, `find_path`, `search_terms`) были нерабочими в штатном production-сценарии (репо обслуживаются демоном, federation-репо на удалённой ноде).

### Как нашли и почему починили сами

Регрессия обнаружена нами **в ходе эксплуатации v0.8.0** (2026-05-06): попытка вызвать `get_object_structure` на любом BSL-репо приводила к `database error: no such table: metadata_objects`, а на federation-репо — к `extension tool '...' currently supports only local repos`. До нас ошибки никто не сообщал — внешние пользователи v0.8.0 могли не дойти до 1С-ветки. Локализовано до двух точек в `code-index-core`: вызовы `apply_schema_extensions` / `index_extras` существовали только в CLI-команде `index` (`cli.rs`), а в `daemon_core/worker.rs` отсутствовали; в `mcp::call_tool` стоял жёсткий отказ для `is_local == false`. После полного цикла проверки (235 unit-тестов + smoke на 4 BSL-репо локально и через federation на VM) — фикс вкатан патчем v0.8.1 без участия внешнего сообщества.

### Исправлено

- **Daemon теперь применяет `schema_extensions` и `index_extras` процессоров.** В v0.8.0 эти вызовы были только в CLI-команде `index <path>`, а worker даемона их не делал. Результат: на любом BSL-репо, проиндексированном через `bsl-indexer.exe daemon run`, BSL-tools падали с `database error: no such table: metadata_objects`. Теперь worker `daemon_core/worker.rs` сам resolve'ит процессор по правилу «явный `language` из `daemon.toml` → fallback `detect()`», применяет `apply_schema_extensions` ДО `full_reindex` (создаёт пустые таблицы — DDL идемпотентен) и вызывает `index_extras` ДО `flush_to_disk` (наполняет таблицы из `Configuration.xml`). Для репо без `Configuration.xml` (например, старые выгрузки обработок) таблицы создаются пустыми — tools отвечают `[]` без exception.
- **Federation теперь форвардит extension-tools на удалённые ноды.** Раньше любой вызов BSL-tool на remote-репо (UT/BP_SS/BP_TDK/ZUP на VM rag) возвращал `extension tool '...' currently supports only local repos`. Введён универсальный route `POST /federate/extension` с payload `{tool_name, args}` — один маршрут на все extension-tools, расширяемо при добавлении новых LanguageProcessor'ов. На source-стороне `mcp::call_tool` форвардит вызов через `dispatcher::dispatch_remote_value`. Обе ноды federation должны быть обновлены до 0.8.1 синхронно — старая нода вернёт 404 на новый route.

### Добавлено

- **`ProcessorRegistry::resolve(explicit_language, repo_root)`** — двухступенчатый resolve процессора: сначала по явному `language` из конфига, потом fallback на `detect()` по маркерам корня. Используется в daemon-worker и в CLI-команде `index`. Унифицирует поведение «индексация» независимо от способа запуска.
- **Структура `mcp::ExtensionToolParams { tool_name, args }`** — payload для federation-форварда extension-tools.
- **Universal handler `handle_extension_tool` в `federation::server`** — находит tool в `extension_tools` snapshot, строит `ToolContext` для local repo и вызывает `IndexTool::execute`. Если на target-ноде такого tool нет (например, она запущена не с bsl-extension) — возвращает `federation_error` с понятным сообщением.

### Изменено

- **`run_worker` принимает `processor_registry: Option<Arc<ProcessorRegistry>>`** (последний параметр). `None` = universal-only сборка (`code-index.exe`); `Some(reg)` = `bsl-indexer.exe`. Используется для resolve процессора текущего репо.
- **`runner::run` принимает `processor_registry`** и пробрасывает в `spawn_worker` (initial loop + `handle_reload`).
- **`cli::handle_daemon` принимает `processor_registry`** — передаётся в `runner::run` при запуске даемона.
- **`Commands::Index` использует `resolve(None, root)`** вместо прямого `detect(root)` — поведение идентично, но единый кодпуть.

### Совместимость

Изменения сигнатур публичных API в `daemon_core::worker`/`runner`/`cli` — additive (новые параметры в конце). Сборка `bsl-indexer` 0.8.1 совместима с конфигом `daemon.toml` 0.8.0 — миграции БД не требуется (DDL `apply_schema_extensions` идемпотентен).

**Federation:** обе ноды нужно обновлять одновременно. До-0.8.1 нода вернёт `404 Not Found` на `POST /federate/extension`, и new-нода покажет это как `federation_error`.

## [0.8.0] — 2026-05-05

**Phase 2 «content для code-файлов»** — закрытие главного ограничения Phase 1. До v0.8.0 `read_file` для `.py`/`.bsl`/`.rs`/`.ts` и других code-файлов возвращал `category="code"` с пустым `content`. Теперь содержимое хранится в новой таблице `file_contents` (zstd-сжатие, миграция v4) и отдаётся при каждом вызове. Дополнительно: новый инструмент `grep_code` для regex-поиска непосредственно по содержимому code-файлов, oversize-механика для файлов крупнее настраиваемого лимита.

### Добавлено

- **Таблица `file_contents` (миграция v4).** DDL: `file_contents(file_id INTEGER PRIMARY KEY REFERENCES files(id) ON DELETE CASCADE, content_blob BLOB, oversize INTEGER NOT NULL DEFAULT 0)`. Backfill автоматический — выполняется в составе `full_reindex` при первом запуске v0.8.0 на существующей БД. Идемпотентна: повторный вызов безопасен (`INSERT OR REPLACE`). Оценка для УТ (~15 665 `.bsl`, ~620 МБ исходников): ~120 МБ blob после zstd (~5×), однократное время backfill ~1-2 минуты (чистый I/O + zstd encode).

- **`read_file` для code-файлов работает в полном объёме.** Для `.py`, `.bsl`, `.rs`, `.ts` и других AST-языков возвращается разжатый content из `file_contents`. `category="code"`. Старая логика чтения text-файлов через `text_files` не меняется.

- **Oversize-механика.** Файлы крупнее `max_code_file_size_bytes` (дефолт **5 МБ**) сохраняются с `oversize=1, content_blob=NULL`. AST-парсинг, FTS и граф вызовов для них работают в полном объёме. `read_file` для oversize-файла возвращает специальный ответ:
  ```json
  {
    "category": "code",
    "content": "",
    "oversize": true,
    "file_size": 8650240,
    "size_limit": null,
    "hint": "Файл oversize: content не сохранён в индексе. Используйте get_function/get_class/grep_body."
  }
  ```

- **`stat_file` показывает `oversize`** для code-файлов: поле `Option<bool>` добавлено в ответ. Для text-файлов — всегда `null`.

- **Конфигурация лимита `max_code_file_size_bytes`.** Hardcoded дефолт — 5 МБ (`DEFAULT_MAX_CODE_FILE_SIZE_BYTES` в `crate::daemon_core::config`). Переопределяется в `daemon.toml`:
  ```toml
  [indexer]
  max_code_file_size_bytes = 5242880   # глобальный override (5 МБ)

  [[paths]]
  path = "C:/RepoUT"
  max_code_file_size_bytes = 10485760  # для этого репо — 10 МБ
  ```
  Приоритет: per-path → секция `[indexer]` → дефолт 5 МБ. Логика выбора — хелпер `PathEntry::effective_max_code_file_size(&IndexerSection)`.

- **Новый MCP-инструмент `grep_code` (Phase 2 bonus).** Regex-поиск по содержимому code-файлов — закрывает слепую зону `grep_body` (тот ищет только в телах функций/классов). Источник данных — таблица `file_contents` (zstd-decode на лету в Rust; SQL делает pre-filter по path/language). Параметры идентичны `grep_text`: `regex`, `path_glob?`, `language?`, `limit?`, `context_lines?`. Файлы с `oversize=1` пропускаются. Storage-метод: `Storage::grep_code_filtered(regex, path_glob, language, limit, context_lines, max_total_bytes) -> Vec<GrepTextMatch>`. Сигнатура pub-функции: `pub async fn grep_code(entry, regex, path_glob, language, limit, context_lines)`.

- **Federation route `/federate/grep_code`** — аддитивный, не ломает существующие клиенты. При обращении к старой ноде (< 0.8.0) вернётся `404` — ожидаемое поведение; обе ноды нужно обновлять синхронно для использования `grep_code` в федерации.

### Изменено

- **`Indexer::write_code_to_db`** — добавлен последний параметр `raw_content: Option<&str>`. Если задан — content сохраняется в `file_contents` (zstd encode). Внутренний API.
- **`Storage::read_file_text`** — добавлен последний параметр `size_limit_bytes: Option<i64>`. Используется для заполнения поля `size_limit` в oversize-ответе. MCP-слой передаёт `None`.
- **`ParsedFile::Code` enum-вариант** — добавлено поле `raw_content: String`.
- **`worker::run_worker`** — добавлен параметр `IndexerSection` (последний). Внутри вычисляется effective лимит и записывается в `IndexConfig.max_code_file_size_bytes`.
- **`runner::spawn_worker`** — добавлен параметр `IndexerSection`, пробрасывается в `run_worker`.

### Безопасность

- **Защита от zstd-bomb.** Все вызовы декомпрессии в `read_file_content` и `grep_code_filtered` идут через приватный helper `Storage::decode_zstd_safe(blob) -> Result<Vec<u8>>`. Использует stream-decoder с `io::Read::take(limit + 1)` — если разжатый размер превысит `FILE_CONTENTS_MAX_DECOMPRESSED_BYTES` (256 МБ), возвращает ошибку, не аллоцирует RAM дальше. 256 МБ заведомо больше любого валидного code-файла (5 МБ default × ~5× zstd = ~25 МБ; запас на случай поднятого `max_code_file_size_bytes` оператором).

### Исправлено

- **Backfill теперь работает для всех code-файлов на стабильной БД (фикс бага первой превью-сборки).** Раньше backfill был встроен в обработку `metadata_updates` в `full_reindex` — это контейнер файлов с изменившимся mtime/file_size, но прежним content_hash. На «стабильной» БД (никто не трогал файлы с прошлой индексации) `metadata_updates` пустой, поэтому backfill **не запускался для UT/BP_SS/ZUP** — наполнялись только репо с реально изменившимися файлами (BP_TDK получал ~15 файлов из 90K). Фикс: вынесено в **отдельную фазу** `Этап 6` после удаления устаревших, через новый Storage-метод `list_code_files_without_content() -> Vec<(file_id, path)>`. Теперь backfill бьёт по всем code-файлам, у которых нет записи в `file_contents` И нет записи в `text_files`, независимо от того менялся ли mtime. Реальные показатели на VM rag после фикса: UT 32599/32599 за 31.7 сек, BP_SS 37535/37535 за 37.9 сек, ZUP 19066/19066 за 17.5 сек, BP_TDK аналогично.
- **Backfill в батчах вместо одной мега-транзакции.** Для 90K-репо вся фаза в `BEGIN TRANSACTION` без commit раздула бы WAL до многих ГБ. Промежуточный `commit_batch + begin_batch` каждые `batch_size.max(500)` файлов держит WAL в разумных пределах.

### Совместимость

- **MCP API без breaking-changes.** Все новые поля в response — `Option<...>` или `default false`; старые клиенты не сломаются. Изменение `read_file` для code-файлов (возвращает реальный content вместо пустого) — улучшение, не breaking.
- **Schema БД** — миграция v4 идемпотентна, безопасна на существующей БД v0.7.x. Откат на v0.7.x просто игнорирует новую таблицу — обе версии совместимы по чтению старых данных.
- **Storage API изменён несовместимо** для прямых пользователей крейта `code-index-core`: `Indexer::write_code_to_db`, `Storage::read_file_text`, `worker::run_worker`, `runner::spawn_worker` — новые параметры. Также добавлены публичные методы: `Storage::upsert_file_content`, `read_file_content`, `has_file_content`, `delete_file_content`, `get_file_id_by_path`, `has_text_file`, `list_code_files_without_content`, `grep_code_filtered`. Внешних callers в публичном API нет, но если есть приватный код с прямыми вызовами — обновить.
- **Federation** — новый route `/federate/grep_code` аддитивный. **Обе ноды federation должны быть обновлены синхронно** для использования `grep_code` в федерации (иначе старая нода вернёт 404 на этот route). Общий принцип `v0.7.0+` остаётся.
- **`grep_code` пропускает oversize-файлы** — это задокументированное ограничение, не баг. Для таких файлов по-прежнему работают `get_function`/`get_class`/`grep_body` по AST-данным.

## [0.7.3] — 2026-05-04

**Bug-fix**: extension-tools (`get_object_structure`, `get_form_handlers` и другие, поставляемые через `LanguageProcessor::additional_tools()`) **не регистрировались в `tools/list`** при работе сервера в федеративном режиме (`serve.toml` присутствует). У пользователей в моно-режиме всё было корректно.

### Исправлено

- **`CodeIndexServer::from_federated`** теперь принимает два дополнительных параметра: `registry: Option<ProcessorRegistry>` и `local_languages: BTreeMap<String, String>`. Реестр процессоров сохраняется в `Self.registry`, и сразу после построения карты репо вычисляется `extension_tools = collect_extension_tools(&active_languages, &reg)`. Раньше федеративный конструктор всегда инициализировал `extension_tools = Vec::new()` и `registry = None`, что обнуляло conditional registration на старте serve и при последующих `reload_extensions` (`registry_opt = None` → `new_tools = Vec::new()`).
- **`local_languages` для federation**: мапа `alias → language` собирается из локального `daemon.toml` (`PathEntry::effective_alias()` + `PathEntry.language`) и проставляется в `RepoEntry.language` для **local-репо**. Без этого `collect_active_languages` не находил bsl/python/rust в federation-сценарии (federation::repos::merge возвращает FederatedRepo без поля language). Remote-репо через federation продолжают приходить без языка — для них extension-tools регистрируются только если такой же язык активен у local-репо на этой ноде.
- **Поведенческое следствие**: на сборке `bsl-indexer` в federation-режиме `tools/list` теперь возвращает 22 инструмента вместо 17 — добавляются `find_path`, `get_event_subscriptions`, `get_form_handlers`, `get_object_structure`, `search_terms` (5 BSL-tools из `bsl-extension`).

### Совместимость

- **MCP API без изменений** — список tool-ов меняется только в federation-режиме сборки `bsl-indexer` при наличии хотя бы одного local-репо с `language = "bsl"` в `daemon.toml`. Клиенту это видно как штатный `notifications/tools/list_changed`.
- **Schema БД без миграций.**
- **Federation требует синхронного апгрейда обеих нод** — общий принцип v0.7.0+ остаётся (cross-node API не менялся, но полезный эффект достигается, только когда обе ноды собраны на 0.7.3).
- Сигнатура `from_federated` изменена несовместимо. Внешних вызовов в публичном API code-index нет (использовалось только из `cli::run`), но если у вас есть приватный код с прямым вызовом — обновите его.

## [0.7.2] — 2026-04-29

**Bug-fix к v0.7.1**: HTML-парсер не подхватывался в репо, у которых в `daemon.toml` явно указан `language="..."` (python/rust/bsl и т.п.). При попытке индексации `.html` файлов выдавалась ошибка `Нет парсера для расширения: html`.

### Исправлено

- **`ParserRegistry::from_languages`** теперь регистрирует HTML-парсер **всегда** дополнительно к указанному `language`. HTML — универсальный ассет (templates, generated docs, sphinx-output, vue/svelte SFC и т.п.), который встречается в репо любого «основного языка» и не указывается отдельно в `daemon.toml`. Ветка `"html" => …` в `match` сохраняется как явный no-op для документирования; реальная регистрация — после `match` безусловно.
- Это устраняет баг при `code-index index <repo> --force` для python-/rust-/bsl-репо с html-файлами.

### Совместимость

- MCP API без изменений.
- Schema БД без изменений.
- Бинарник 0.7.1 без этого фикса в production может оставаться — html-файлы просто не получат AST-записей до 0.7.2 + переиндексации.

## [0.7.1] — 2026-04-28

**HTML-парсер** через tree-sitter — добавлен **по запросу пользователя**. До 0.7.1 `.html` индексировался только как text-файл (FTS+regex+read_file). Теперь — полноценный AST с извлечением структурных сущностей: элементы с id, формы, поля ввода, ссылки, inline-скрипты/стили, CSS-классы. Сохранена обратная совместимость: search_text/grep_text/read_file для html продолжают работать через **двойную индексацию** (text_files + AST).

### Добавлено

- **Новый парсер** `crates/code-index-core/src/parser/html.rs` (~430 строк) на основе `tree-sitter-html` 0.23. Поддерживает `.html` и `.htm`. Зарегистрирован в `ParserRegistry::new_all()` и `from_languages()`.
- **Маппинг семантики HTML → таблицы code-index:**

  | HTML-конструкция | → | Таблица | Имя |
  |---|---|---|---|
  | `<element id="X">…</element>` | `classes` | `X` (body=outerHTML, bases=tag_name) |
  | `<form id|name="X">` | `classes` | `form_X` (bases="form") |
  | `<form>` без id/name | `classes` | `form_<line>` |
  | `<input/select/textarea name="Y">` | `variables` | `Y` (value=type/value-атрибут) |
  | `<a href="URL">` | `imports` | `module=URL`, `kind="link"` |
  | `<link href="URL" rel="X">` | `imports` | `module=URL`, `kind=X` (или "stylesheet") |
  | `<script src="URL">` | `imports` | `module=URL`, `kind="script"` |
  | `<img/iframe/video/audio/source/embed src="URL">` | `imports` | `module=URL`, `kind=tag_name` |
  | `<script>…inline JS…</script>` | `functions` | `inline_script_<line>` (body=содержимое) |
  | `<style>…inline CSS…</style>` | `functions` | `inline_style_<line>` (body=содержимое) |
  | Атрибут `class="foo bar baz"` | `variables` | `class:foo`, `class:bar`, `class:baz` (по одной записи на каждый) |

- **Двойная индексация**: для языков из `is_dual_indexed_language()` (на 0.7.1 — только `html`) при индексации параллельно создаётся запись в `text_files`. Это сохраняет работоспособность `search_text`/`grep_text`/`read_file` для HTML-файлов наряду с новыми structured queries (`get_class("cart")`, `find_symbol("submitOrder")`, `get_imports(module="bootstrap.css")` и т.п.). Реализовано через новое поле `text_for_fts: Option<String>` в `ParsedFile::Code` + дополнительный параметр `text_for_fts: Option<&str>` в `Indexer::write_code_to_db`.
- **Расширения файлов**: `("html", "html")` и `("htm", "html")` перенесены из TEXT_EXTENSIONS в CODE_EXTENSIONS (`indexer/file_types.rs`). Добавлена публичная функция `is_dual_indexed_language(language: &str) -> bool`.
- **13 unit-тестов** для парсера html (`parser/html.rs::tests`): id-элемент, форма с id/name/без обоих, input/select/textarea, link/script/img imports, inline-скрипт, inline-стиль, classes-атрибут, толерантность к Jinja-шаблонам, пустой HTML, вложенные элементы. Plus `file_types::html_is_code_with_dual_indexing` для проверки категоризации.
- **Tolerance к шаблонизаторам**: `{{ … }}` и `{% … %}` парсятся как text-content без падения. Структурные элементы вокруг них извлекаются нормально.

### Изменено

- **Сигнатура `Indexer::write_code_to_db`**: добавлен последний параметр `text_for_fts: Option<&str>`. Внутренний API, не MCP-видимый. Все известные callers (worker.rs:380 для html, worker.rs:416 для xml_1c) обновлены.

### Совместимость

- **MCP API без изменений** — никаких новых tool-ов, никаких новых параметров. После переиндексации html-файлы автоматически становятся доступны для существующих tool-ов: `get_class`, `find_symbol`, `search_function`, `get_imports`, `grep_body` + продолжают работать `search_text`, `grep_text`, `read_file`, `list_files`, `stat_file`.
- **Schema БД без миграций.** Используются существующие таблицы files / functions / classes / imports / variables / text_files. Двойная вставка для html — через прежний `insert_text_file`.
- **Federation без новых routes.** Внутренний механизм; обе ноды должны быть одной версии (требование 0.7.0 продолжает действовать).
- **Переиндексация:** при первом запуске v0.7.1 daemon найдёт mtime html-файлов неизменным относительно прошлой индексации и **не будет** их переиндексировать (mtime pre-filter из v0.4.0). Чтобы получить новые structured-записи для уже индексированных html, нужен либо явный re-index (`code-index index <repo>`), либо изменение mtime файла. Рекомендуется при первом обновлении на 0.7.1 — однократный полный re-index репо с html-файлами.

## [0.7.0] — 2026-04-28

**Phase 1 «read-only tools»** — закрытие пробелов code-index, чтобы удалённый репо через federation работал «как локальный» для большинства задач разведки и чтения. Релиз read-only: схема БД не трогается, переиндексация не нужна, обратная совместимость сохранена.

### Добавлено

- **`stat_file(repo, path)`** — метаданные одного файла из таблицы `files`. Возвращает `{exists, path, language, size, mtime, lines_total, content_hash, indexed_at, category}`. `category` — `"text"` (содержимое доступно через `read_file`) или `"code"` (Phase 1 не хранит content для AST-языков).
- **`list_files(repo, pattern?, path_prefix?, language?, limit?)`** — плоский список файлов с фильтрацией. `pattern` — glob (`**/*.py`), `path_prefix` — префикс (`src/auth/`). Возвращает `[{path, language, lines_total, size, mtime}]`. Без отдельного `tree`-эндпоинта — структура восстанавливается по path-строкам.
- **`read_file(repo, path, line_start?, line_end?)`** — содержимое **text-файла** (yaml/md/json/toml/xml/sh/INI/CSV/SQL и др.) через таблицу `text_files`. `line_start`/`line_end` — 1-based, inclusive. Soft-cap **5000 строк ИЛИ 500 КБ** (что наступит раньше) с флагом `truncated=true`. Hard-cap **2 МБ** даже с диапазоном (отказ). Для code-файлов — `category="code"` и пустой `content` (закроется в Phase 2). Возвращает `{content, lines_returned, lines_total, truncated, indexed_at, category}`.
- **`grep_text(repo, regex, path_glob?, language?, limit?, context_lines?)`** — regex-поиск по содержимому text-файлов через REGEXP. Закрывает дыру FTS5 со спецсимволами (точки, скобки, экраны). `path_glob` или `language` желателен — иначе full-scan, default-limit занижен до 100. `context_lines` — N строк до/после совпадения. Hard-cap на суммарный размер выдачи (1 МБ).
- **`path_glob`-параметр** в `search_function`, `search_class`, `get_function`, `get_class`, `find_symbol`, `search_text`, `grep_body`. Сужает выдачу по пути файла. Реализация — post-filter через crate `globset` после SQL-выборки. SQL-LIMIT увеличивается до 5× (но не больше 500), чтобы фильтр не оставил пустую выдачу.
- **`context_lines`-параметр** в `grep_body` — N строк контекста вокруг первых до 3 совпадений. Через новый `Storage::grep_body_with_options`. Существующий `grep_body` без context-параметра работает как раньше (обратная совместимость для cli.rs/тестов).
- **Hard-cap на суммарный размер ответа** в `grep_body` (с context_lines) и `grep_text` — 1 МБ. Защита от переполнения контекста модели на широком regex с большим context_lines.
- **`Storage::get_path_by_file_id`** — публичный метод для post-filter в MCP-слое.
- **`storage::normalize_glob`** (pub(crate)) — `**` → `*` для совместимости с привычным glob-синтаксисом (SQLite GLOB и `globset` уже понимают `*` как multi-char + `/`).
- **Federation routes:** `/federate/stat_file`, `/federate/list_files`, `/federate/read_file`, `/federate/grep_text`. Существующие routes расширены новыми параметрами в Params-структурах.
- **20 новых unit-тестов** для Phase 1: `normalize_glob`, `slice_with_caps` (4 кейса), `stat_file_meta` (3 кейса), `list_files_filtered` (3 кейса), `read_file_text` (4 кейса), `grep_text_filtered` (3 кейса), `grep_body_with_options`, `get_path_by_file_id`.

### Совместимость

- **MCP API без breaking-changes.** Все новые параметры — `Option<...>`, опциональные. Старые клиенты, не знающие о `path_glob`/`context_lines`, работают как раньше.
- **Storage API без breaking-changes.** Все существующие методы (`search_functions`, `search_classes`, `search_text`, `grep_body`, `find_symbol`) сохранили сигнатуру. Новая функциональность — в новых методах (`grep_body_with_options`) и в post-filter в MCP-слое.
- **Schema БД без изменений.** Никаких миграций, переиндексации не требуется.
- **Federation без breaking-changes.** Новые routes аддитивны. **Важно:** обе ноды federation (Windows и VM) должны быть обновлены до 0.7.0 одновременно — иначе при вызове новых tool-ов на старой ноде будет 404.

### Известные ограничения Phase 1

- **`read_file` для code-файлов** (.py/.rs/.bsl/.ts/...) возвращает `category="code"` и пустой `content`. Закроется в Phase 2 миграцией v4 + zstd-compressed blob в новой таблице `file_contents`.
- **Файлы без расширения** (Dockerfile, Makefile, Jenkinsfile, .gitignore, LICENSE) не индексируются walker-ом — слепая зона DevOps-репо. Сознательное ограничение.
- **Бинарные форматы 1С** (.epf, .erf, .cfe, .cf) не индексируются. Распаковка во внешнем pipeline.

## [0.6.1] — 2026-04-26

Технический долг rc7 закрыт: per-host порт удалённого `code-index serve` для federate-форвардинга. До 0.6.0 включительно порт удалённой ноды был захардкожен в `client.rs::DEFAULT_REMOTE_PORT = 8011`, и две serve-ноды на одной машине неизбежно перекрывали друг друга в connection pool — пара ключевалась только по IP. Изменение полностью обратно совместимое: `serve.toml` без поля `port` работает ровно как раньше (используется дефолт 8011).

### Добавлено

- **Поле `port: Option<u16>`** в `[[paths]]` секции `serve.toml` (`federation::config::ServePathEntry`). Опциональное, default — `DEFAULT_REMOTE_PORT` (8011). Метод `effective_port()` возвращает явное либо дефолт. Валидация запрещает `port = 0` (зарезервирован).
- **Поле `port: u16`** в `federation::repos::FederatedRepo` и `mcp::RepoEntry` — обязательное, заполняется из `ServePathEntry::effective_port()` при `merge`. Для local-записей значение информационное (форвардинг для них не используется).
- **Тесты:** `port_field_is_optional_and_defaults_to_remote_port`, `port_field_parses_when_explicit`, `zero_port_fails_validation` (config.rs), `port_defaults_when_not_set_and_propagates_when_set` (repos.rs), `pool_creates_separate_clients_for_different_ports_on_same_ip` (client.rs).

### Изменено

- **`RemoteClientPool` ключует клиентов по `(String, u16)`** вместо `String`. Сигнатура `get_or_create(&self, ip: &str, port: u16)`. Поле `default_port` убрано: пул сам по себе порт не фиксирует, он подаётся per-call из `RepoEntry::port`. `RemoteClientPool::new(timeout)` теперь принимает только таймаут.
- **`dispatcher::dispatch_remote` и `dispatch_remote_value` принимают `port: u16`** между `ip` и `tool`. Все 13 tool-handler-ов (`mcp/mod.rs`) и `tools::remote_stats` обновлены — пробрасывают `entry.port`.

### Совместимость

- **`serve.toml` без поля `port`** парсится как раньше, для всех записей используется `DEFAULT_REMOTE_PORT`. Никаких миграций не требуется.
- **Внешний MCP API без изменений** — поле `port` не появляется ни в одном tool-call, ни в одном tool-result. Это деталь конфигурации serve, наружу не уходит.
- **Кэширующий прокси (планируется)** будет читать `serve.toml` для определения, на какой `port` ходить к каждому репо — теперь это единая точка истины.

## [0.6.0] — 2026-04-26

Большой релиз: workspace-рефакторинг, новый бинарник `bsl-indexer` с полной 1С-спецификой, multi-config обработка одного репо с base/ + extensions/, парсинг `ConfigDumpInfo.xml` для UUID-идентификаторов отладки, опциональное LLM-обогащение процедур через cargo feature `enrichment`, защита от рассинхрона моделей через `embedding_signature`. Все наработки сделаны на ветке `workspace-refactor` (24+ коммита, 249 тестов).

### Добавлено

- **Cargo Workspace**. Один моно-крейт превращён в 4 крейта с чёткими зонами ответственности:
  - `code-index-core` (lib, publish=true) — универсальное ядро: file scanner, tree-sitter-парсеры (Python/Rust/Go/Java/JS/TS/BSL), SQLite-схема, MCP-сервер, federation.
  - `code-index` (bin, publish=true) — публичный бинарник без 1С-специфики.
  - `bsl-extension` (lib, publish=false) — 1С-специфика: XML-парсеры выгрузки, граф вызовов BSL, MCP-tools `get_object_structure`/`get_form_handlers`/`get_event_subscriptions`/`find_path`/`search_terms`, опциональный LLM-enrichment.
  - `bsl-indexer` (bin, publish=false) — приватный бинарник = core + bsl-extension. Используется на VM RAG для индексации конфигураций 1С.

- **Conditional MCP-tool registration**. MCP-сервер на старте читает `daemon.toml`, для каждого `[[paths]]` определяет `language` (явно или auto-detect по корню репо), собирает множество активных языков и регистрирует ТОЛЬКО tools от подходящих `LanguageProcessor`-ов. Если в репо нет ни одного BSL-репозитория — 1С-инструменты вообще не появляются в `tools/list`. Уведомление `notifications/tools/list_changed` отправляется при правке `daemon.toml` (file-watch с debounce 500мс через `notify-debouncer-full`).

- **`bsl-indexer` — новый отдельный бинарник** для конфигураций 1С. Релиз CI собирает его под Windows/Linux/macOS (с feature `enrichment` для прода). Подробная инструкция — в [docs/bsl-indexer.md](docs/bsl-indexer.md), деплой на VM RAG — [docs/deploy-vm-rag.md](docs/deploy-vm-rag.md).

- **Multi-config layout** (`<repo>/base/Configuration.xml` + `<repo>/extensions/<EF_*>/Configuration.xml`). `BslLanguageProcessor::detects()` теперь рекурсивно (глубина ≤ 2) находит любой `Configuration.xml`. `index_metadata_objects` обходит ВСЕ найденные конфигурации в дереве и сводит их объекты в одну таблицу (заимствованные в расширениях объекты пропускаются через `INSERT OR IGNORE`). `extension_name` хранится для каждого модуля — фильтр между base и CFE доступен запросом.

- **Таблица `metadata_modules`** с тройкой UUID для платформенного отладчика 1С (`dbgs-debug` setBreakpoint):
  - `object_id` — UUID объекта/формы из атрибута `uuid` корневого элемента в его XML.
  - `property_id` — UUID типа модуля (Module/ManagerModule/FormModule/...) — константа платформы, словарь в `module_constants.rs`.
  - `config_version` — хеш версии из `ConfigDumpInfo.xml` (отдельный парсер). Меняется при каждом изменении конфигурации.

  Эта тройка позволяет агентам ставить breakpoint'ы по человекочитаемому имени модуля, не дёргая live-ИБ. На УТ-масштаб ~8 тыс модулей, на BP-конфигурациях ~10 тыс.

- **MCP-tool `search_terms`** — третий канал семантического поиска (после `search_function` и будущего `semantic_search`). Использует FTS5 на колонке `procedure_enrichment.terms`, заполняемой LLM-обогащением. Поддерживает FTS-синтаксис (AND, OR, NOT, "точная фраза", префикс*). NULL-записи (необогащённые процедуры) просто не находятся — это progressive enhancement, не баг.

- **Подкоманда `bsl-indexer enrich [--path P] [--limit N] [--reenrich]`** под cargo feature `enrichment`. HTTP-клиент к OpenAI-compatible chat-completions endpoint (OpenRouter / Ollama / любой совместимый). Параллельная обработка через `tokio::task::JoinSet` с настраиваемым `batch_size`. Защита от рассинхрона моделей через `embedding_meta.enrichment_signature` — при смене модели в конфиге выводится warning с предложением `--reenrich`.

- **Секция `[enrichment]` в `daemon.toml`** — провайдер, URL endpoint, имя модели, имя env-переменной API-key, batch-size, шаблон промпта. По умолчанию выключено (фича опциональная).

- **Auto-detect языка с записью обратно в `daemon.toml`** через `toml_edit` (сохраняет комментарии). Алгоритм: `Configuration.xml` → bsl, `pyproject.toml`/`setup.py` → python, `Cargo.toml` → rust, `package.json` → javascript/typescript, иначе по преобладанию расширений. Если эвристика не сработала — warning в лог и пропуск (без молчаливого фолбэка).

- **`Storage::apply_schema_extensions(extensions: &[&str])`** — точка применения дополнительных DDL от LanguageProcessor'ов. Вызывается один раз при первом открытии БД репо для языка, требующего специфичных таблиц.

- **`LanguageProcessor::index_extras(repo_root, &mut storage)`** — hook для специфичных постобработок после основной индексации (например, парсинг XML и заполнение `metadata_*`-таблиц). Дефолтная реализация — no-op.

### Изменено

- **Параллельный прогон 4 репо на VM RAG (8 ядер Intel Xeon)** — суммарное время полной индексации УТ + BP_1 + BP_2 + ZUP уменьшилось с ~8м30с (последовательно) до **3м11с** (×2.7 выигрыш). Узкое место — single-thread SQLite FTS-rebuild у каждого процесса; диск (NVMe) не блокирует, разница холодный↔горячий кеш всего ~5 сек.

- **Защита от cascade-ошибок транзакций**. В каждой `index_*`-функции и `build_call_graph` добавлен идемпотентный `ROLLBACK` перед `BEGIN` — если предыдущая функция оставила открытую транзакцию, следующая корректно её закроет вместо падения с «cannot start a transaction within a transaction».

- **`config_watch::run_watch` — первичная затравка active_languages при старте**. До правки клиент, подключившийся ДО первого изменения файла, видел только core-tools (потому что в моно-режиме `RepoEntry.language=None` при загрузке через `cli::run`). После правки — первый `tools/list` сразу содержит правильный набор для текущего `daemon.toml`.

- **Настройка CI**. `.github/workflows/release.yml` теперь собирает 6 артефактов на каждый tag: `code-index` × {Windows, Linux, macOS} + `bsl-indexer` × {Windows, Linux, macOS} (с `--features enrichment`). Кеш cargo registry/git/target по `${{ runner.os }}-${{ matrix.target }}-${{ matrix.crate }}`.

### Безопасность

- **`.mcp.json` исключён из tracking** через `.gitignore` + `git rm --cached`. Файл — локальная конфигурация, содержит SSH-пути и URL'ы конкретного хоста; в репо ему не место.

- **Внутренние IP заменены на RFC 5737 doc-IP** (`192.0.2.0/24`) во всех тестах federation, комментариях и примерах конфигов. Конкретные адреса VM RAG в инструкции деплоя — на placeholder `<vm-rag-ip>`.

### Эмпирическая верификация на проде (этап 7-8)

- **Conditional registration на Claude Code 2.1.120** — `tools/list` корректно содержит 18 tools (5 BSL + 13 core) при наличии BSL-репо в `daemon.toml`, 13 tools (только core) без них.
- **`notifications/tools/list_changed` Claude Code на 2.1.120 ИГНОРИРУЕТ** — баг [anthropics/claude-code#13646](https://github.com/anthropics/claude-code/issues/13646) подтверждён эмпирически. Workaround — ручной `/mcp Reconnect`. Reconnect (issue #33779) на 2.1.120 уже корректно перечитывает `tools/list`.
- **VM RAG (Linux, 8 ядер, NVMe)** — RepoUT 53.6 с холодным кешем, 57.7 с горячим, разница 5 сек = диск не bottleneck. Параллельная индексация всех 4 репо за 3м11с при 8 ядрах × ~2 ядра rayon на процесс.

### Документация

- **[docs/bsl-indexer.md](docs/bsl-indexer.md)** — пользовательская инструкция по `bsl-indexer`: что умеет, как собрать с/без feature `enrichment`, как настроить enrichment с OpenRouter / Ollama, ограничения MCP-клиентов с workaround'ом.
- **[docs/bsl-indexer-architecture.md](docs/bsl-indexer-architecture.md)** — полное архитектурное ТЗ workspace-refactor с обоснованиями решений.
- **[docs/deploy-vm-rag.md](docs/deploy-vm-rag.md)** — пошаговая инструкция деплоя на VM (установка Rust toolchain, копирование исходников, настройка daemon.toml, systemd-unit, A/B-протокол сравнения с pg_indexer).
- **[deploy/systemd/bsl-indexer-daemon.service](deploy/systemd/bsl-indexer-daemon.service)** — готовый systemd-unit с лимитами ресурсов и защитой от записи вне разрешённых каталогов.

## [0.5.0-rc6] — 2026-04-25

### Добавлено

- **Федеративная архитектура `code-index serve`** (по образцу `1c-router`/`mcp__1c__`). Один процесс serve обслуживает реестр репозиториев из нескольких машин: для каждого tool-call с `repo=X` локальный serve смотрит ip — если совпадает с `[me].ip`, читает локальный SQLite, иначе делает HTTP-вызов к удалённому serve. Источник истины для каждого репо — на одной машине (это прокси, не репликация).

  **Новый конфиг** [`serve.toml`](src/federation/config.rs) — глобальный, одинаковый на всех нодах (раскатывается через общий git-репо `code-index-config`):

  ```toml
  [me]
  ip = "192.0.2.10"
  # token = "..."   # опционально, в rc6 не валидируется (заготовка под rc7)

  [[paths]]
  alias = "ut"
  ip = "192.0.2.50"

  [[paths]]
  alias = "dev"
  ip = "192.0.2.10"
  ```

  `daemon.toml` остаётся локальным (только пути этой машины, без изменений в схеме).

- **Внутренний endpoint `POST /federate/<tool_name>`** ([`src/federation/server.rs`](src/federation/server.rs)) — приёмная сторона форвардинга. Тело запроса — JSON, точно соответствующий нашим `*Params`-структурам. Ответ — то же, что вернул бы локальный tool-handler. `/federate` живёт на том же axum-роутере, что `/mcp`, защищён общим whitelist middleware.

- **IP-whitelist middleware** ([`src/federation/whitelist.rs`](src/federation/whitelist.rs)). serve биндится на `[me].ip` (не на `127.0.0.1`, не на `0.0.0.0`) — порт активен только на одном интерфейсе. Допустимые peer-IP — из `{все [[paths]].ip} ∪ {127.0.0.1, ::1}`. Чужой peer → `403 {"error":"forbidden","peer":"..."}`.

- **Параллельный fan-out у `get_stats(repo=None)`** ([`src/mcp/tools.rs`](src/mcp/tools.rs)) через `tokio::task::JoinSet`. Каждый remote-репо опрашивается с таймаутом 5 сек; недоступные возвращаются как `{"repo":"...","status":"unreachable","error":"..."}`, не блокируя остальные.

- **Флаг `--serve-config <FILE>` у `code-index serve`**. Если флаг не задан — ищется `$CODE_INDEX_HOME/serve.toml`. Если файла нет — serve работает как rc5 (моно-режим, bind 127.0.0.1, без whitelist). При `transport=stdio` или явном `--path` федерация не активируется.

  ```bash
  # Федеративный режим (rc6+):
  code-index serve --transport http --port 8011

  # Совместимый rc5-режим (моно):
  code-index serve --transport http --port 8011 --path ut=C:/RepoUT
  ```

- **Пул переиспользуемых HTTP-клиентов** ([`src/federation/client.rs`](src/federation/client.rs)) — один `reqwest::Client` на удалённый IP, lazy init через `RemoteClientPool::get_or_create`. Таймаут 5 сек; idle pool 60 сек.

### Изменено

- **`RepoEntry` теперь хранит `ip` и `is_local`**, поля `root_path` и `storage` обёрнуты в `Option` (`None` для remote). Старые конструкторы `open_readonly_multi` / `open_readonly` / `with_storage` ставят `is_local=true`, `ip="127.0.0.1"` — обратная совместимость для тестов и моно-режима.

- **`serve_http` принимает опциональные `federate_router` и `whitelist`**. Если переданы — `Router::merge` для `/federate/*` и `axum::middleware::from_fn_with_state` для whitelist. Listener теперь использует `into_make_service_with_connect_info::<SocketAddr>()` — без этого peer-IP в middleware не извлечётся.

- **`--host` стал `Option<String>`**. Если задан — приоритет CLI; иначе при наличии serve.toml — `[me].ip`, иначе `127.0.0.1` (rc5-default).

### Защита от циклов

- **Никаких заголовков** типа `X-Forwarded-Already`. Защита — статически по конфигу: каждая нода знает свой `[me].ip`, форвардит только если `repo.ip != own_ip`. При расхождении конфигов (`A: X→B`, `B: X→A`) запрос упадёт по таймауту 5s с понятной ошибкой.
- Приёмник `/federate/get_stats` без `repo` ограничивает fan-out только своими local-репо (не делает рекурсивный обход к чужим), чтобы исключить круг между нодами.

### Roadmap (вне rc6)

- Создание git-репо `code-index-config` с шаблоном `serve.toml` — операционная задача.
- Linux-бинарник + systemd unit для деплоя на VM 200.
- `[me].token` авторизация — Bearer header в `/federate/*`, проверка в whitelist-middleware. Поле уже парсится в схеме serve.toml.
- HEAD-ping к удалённым в `health` — низкоприоритетная фича.
- Hot-reload `serve.toml` без рестарта (`POST /reload` для serve).

## [0.5.0-rc5] — 2026-04-22

### Добавлено

- **HTTP-транспорт у `code-index serve`** через rmcp `StreamableHttpService`. Один процесс обслуживает все репозитории под `mcp-supervisor`, клиенты подключаются к общему URL без копирования `--path` в каждый `.mcp.json`.

  ```bash
  # stdio (per-session, как раньше)
  code-index serve --path ut=C:/RepoUT --path bp=C:/RepoBP

  # http (shared process)
  code-index serve --transport http --port 8011 --config C:/tools/code-index/daemon.toml
  ```

  Клиентский `.mcp.json`:
  ```json
  "code-index": { "type": "http", "url": "http://127.0.0.1:8011/mcp" }
  ```

  Реализация — [`src/main.rs`](src/main.rs) `serve_http`: `StreamableHttpService::new(factory, LocalSessionManager, StreamableHttpServerConfig::default())`, монтируется в `axum::Router::nest_service("/mcp", svc)`. Фабрика клонирует уже собранный `CodeIndexServer` (он `Clone`), так что все сессии разделяют общий набор открытых SQLite-баз.

- **Multi-repo в одном serve-процессе**. `--path` теперь принимает `alias=dir` и может указываться многократно — каждый tool-call передаёт параметр `repo=<alias>` для выбора репозитория. Без `=` — старый контракт `alias=default`. Параметры инструментов получили поле `repo: String`, внутренняя структура `RepoEntry` держит открытую read-only `Storage` и `root_path` per-репо.

- **Поле `alias` в `[[paths]]` daemon.toml** — [`src/daemon_core/config.rs`](src/daemon_core/config.rs) `PathEntry::alias: Option<String>`. Если не задан — алиас вычисляется через `PathEntry::effective_alias()` из последнего сегмента пути (нижний регистр, пробелы → `_`). Демон поле игнорирует; использует только `code-index serve --config ...` при сборке списка репо. Старые конфиги без `alias` продолжают работать (`#[serde(default)]`).

- **Флаги `--host`, `--port`, `--config` у `serve`**. `--config` указывает на `daemon.toml` — список репо и алиасов берётся оттуда. CLI `--path` имеет приоритет над конфигом. Порт по умолчанию — 8011 (следующий свободный в диапазоне mcp-supervisor: 8001/8002/8007/8010).

### Зависимости

- Включена фича `rmcp/transport-streamable-http-server` (подтянула `transport-streamable-http-server-session`, `server-side-http`, транзитивно — `uuid`, `sse-stream`). `axum` и `tower` уже были в deps для health-endpoint демона.

## [0.5.0-rc4] — 2026-04-17

### Исправлено

- **Демон падал при закрытии консоли на Windows**. `code-index` собирается как console-subsystem приложение: при запуске в пользовательской сессии (Scheduled Task c `LogonType=Interactive`, ручной вызов из `cmd`/PowerShell) процесс получает консольное окно и становится его дочерним. Закрытие окна отправляет `CTRL_CLOSE_EVENT`, и демон уходит вместе с ним. Для штатной установки через `scripts/install-daemon-autostart.ps1` это означало, что окно консоли всплывало при logon и любое его закрытие останавливало индексацию.

  **Фикс**: в [`src/main.rs`](src/main.rs) `handle_daemon` при `daemon run` на Windows выполняет self-detach — перезапускает себя с флагами `DETACHED_PROCESS | CREATE_NO_WINDOW`, выставляет переменную окружения `CODE_INDEX_DAEMON_DETACHED=1` и завершает родительский процесс. Detached-клон работает без консоли и переживает закрытие любой родительской сессии. На Unix self-detach не выполняется — демонизацией управляет `systemd`/`launchd`.

  Реализация использует только `std::os::windows::process::CommandExt::creation_flags`, новых зависимостей не добавляет.

## [0.5.0-rc3] — 2026-04-17

### Исправлено

- **Race condition при atomic-save редакторов**. Редакторы (VS Code, IDE, `git`) сохраняют файлы атомарно: сначала пишут во временный `<имя>.tmp.<pid>.<ts>`, затем переименовывают в целевой файл. Watcher через `ReadDirectoryChangesW` успевал увидеть событие `Create` на `.tmp`-файле, но к моменту вызова `hasher::file_hash()` файл уже был переименован. В логи лилась стена ошибок вида `file_hash \\?\...\.mcp.json.tmp.10296.1776427368309: Не удается найти указанный файл. (os error 2)`.

  **Фикс**: в [`daemon_core/worker.rs`](src/daemon_core/worker.rs) `apply_event` при `io::ErrorKind::NotFound` от `file_hash` теперь тихо выходит из обработчика. Логируются только настоящие ошибки (permission denied, read error и т.п.).

### Добавлено

- **Поле `exclude_file_patterns` в `.code-index/config.json`** — glob-паттерны имён файлов для исключения из индексации. Дополняет существующее `exclude_dirs`:

  ```json
  {
    "exclude_dirs": [".vscode", "experimental"],
    "exclude_file_patterns": ["*.tmp.*", "*.bak", "*.orig", "*.swp"]
  }
  ```

  Паттерны матчатся по **basename** (имя файла без пути). Применяются:
  - в [`watcher.rs`](src/watcher.rs) — события от файлов, попавших под паттерн, отбрасываются до вызова `file_hash`;
  - в [`indexer/mod.rs`](src/indexer/mod.rs) `collect_candidates` / `collect_candidates_standalone` — файл исключается из обхода WalkDir до категоризации.

  Синтаксис glob через crate `globset`. Некорректные паттерны логируются и пропускаются (не ломают старт).

### Зависимости

- Добавлен `globset = "0.4"`.

## [0.5.0-rc2] — 2026-04-17

### Исправлено

- **WAL-файлы разрастались до десятков ГБ в production**. После сутки работы на нашем стенде с 13 индексируемыми папками (5 крупных 1С-репо + 8 модулей MCP) WAL-файлы заняли ~43 ГБ при суммарном размере основных БД ~16 ГБ:

  | Репо | `index.db` | `index.db-wal` (до фикса) |
  |------|-----------|---------------------------|
  | RepoBP_2 | 4.7 ГБ | **19 ГБ** |
  | RepoUT | 2.1 ГБ | **17 ГБ** |
  | RepoZUP | 5.1 ГБ | 5.1 ГБ |
  | dbgs-debug | 1.4 ГБ | 1.4 ГБ |

  Свободное место на системном диске сократилось на ~45 ГБ за сутки.

  **Причина**, найденная анализом кода:
  1. `PRAGMA wal_autocheckpoint=500` (добавлен в v0.5.0-rc1) переносит страницы из WAL в основную БД, но **не уменьшает физический файл WAL** — это делает только явный `PRAGMA wal_checkpoint(TRUNCATE)`.
  2. На bulk-нагрузке (initial reindex 90К файлов, частые watcher-batch'и) checkpoint не успевает за темпом записи.
  3. В worker'е после каждого batch вызывался `Storage::flush_to_disk()` через `Connection::backup()` — в disk-режиме (а worker в нём всегда после reopen) это бесполезное копирование БД самой в себя, WAL не уменьшается.

  **Фикс**:
  - Добавлен метод `Storage::checkpoint_truncate()` — обёртка над `PRAGMA wal_checkpoint(TRUNCATE)`, реально схлопывает WAL.
  - В `worker.rs` после initial reindex (когда worker гарантированно в disk-режиме) — обязательный `checkpoint_truncate`. Это самый «жирный» источник WAL.
  - В watcher-цикле после `commit_batch` — `flush_to_disk` заменён на `checkpoint_truncate`. На graceful shutdown — то же самое.

  **Результат проверки на тех же 13 папках**: WAL остаётся **0 байт** после initial reindex и после правок файлов через watcher. Освобождено ~48 ГБ.

## [0.5.0-rc1] — 2026-04-17

Крупная переработка архитектуры: разделение на **фоновый демон-писатель** и **MCP-читателей**.

### Ломающие изменения

- **`code-index serve` теперь read-only**. Он больше не индексирует и не держит watcher — только подключается к БД, которую поддерживает отдельный демон. Если демон не запущен или папка не в его конфиге, tool-call возвращает структурированный ответ `{"status": "daemon_offline" | "not_started" | "indexing", ...}`, а не падает.
- **Per-project PID-lock удалён** (файл `.code-index/serve.pid` больше не создаётся). Сколько угодно MCP-процессов могут подключаться к одной `.code-index/index.db` параллельно.
- **Флаги `--no-watch`, `--flush-interval`** у `serve` удалены — они были специфичны для прежней роли writer'а и к read-only неприменимы.

### Добавлено

- **Подкоманда `daemon`**: `code-index daemon run/start/stop/status/reload`. `run` — foreground (для Scheduled Task / systemd), `start`/`stop`/`status`/`reload` — HTTP-клиент к запущенному демону.
- **Переменная окружения `CODE_INDEX_HOME`** — единая точка конфигурации. Содержит `daemon.toml`, туда же кладутся runtime-файлы `daemon.pid`, `daemon.json`, `daemon.log`. Работает и через системную переменную (`setx`), и через блок `"env"` в `.mcp.json`.
- **Конфиг `daemon.toml`** со списком отслеживаемых папок и параметрами:
  - `max_concurrent_initial` — сколько папок одновременно в фазе initial reindex (дефолт `1`, защита от RAM-всплеска).
  - Per-folder `debounce_ms` / `batch_ms` — переопределение watcher-задержки per-проект.
- **HTTP health-IPC на loopback**: `GET /health`, `GET /path-status?path=...`, `POST /reload`, `POST /stop`. Порт выбирается автоматически и пишется в `daemon.json`.
- **Per-folder жизненный цикл**: `not_started → initial_indexing → ready ⇄ reindexing_batch | error`. Видно в `daemon status`.
- **PowerShell-скрипт** `scripts/install-daemon-autostart.ps1` для установки Scheduled Task (триггер — вход пользователя, автоматически делает `setx CODE_INDEX_HOME`).

### Изменено

- **Память**: только один in-memory SQLite-storage одновременно. После initial reindex worker делает flush → переоткрывает тот же файл в disk-режиме (WAL) → освобождает permit семафора. Пиковая RAM не суммируется по папкам.
- **Повторный запуск**: если `.code-index/index.db` уже существует, worker открывает её сразу в disk-режиме (пропуская backup disk→memory→disk). На 2 1С-репо по ~90К файлов повторный старт — **~12 с** (раньше ~600 с при том же коде до фикса).
- **SQLite**: добавлены `PRAGMA wal_autocheckpoint=500` и `PRAGMA journal_size_limit=67108864` — WAL-файл не раздувается за длинные транзакции, truncate'ится до 64 МБ после checkpoint.
- **MCP-сервер** проверяет статус папки у демона перед каждым tool-call. Если папка не `ready` — возвращает структурированный JSON с прогрессом/подсказкой, а не эмпирический результат из неактуального индекса.

### Удалено

- Legacy-модули `src/daemon.rs` и `src/pidlock.rs`.

### Замеры (1С-репо, 2 папки по 88–92К файлов, 80% XML)

| Сценарий | Время |
|----------|-------|
| Initial reindex с нуля, обе папки последовательно (`max_concurrent_initial=1`) | ~10 мин, RAM-пик ~6 ГБ |
| Повторный старт на существующей БД | ~12 с |
| Watcher: от правки файла до появления в индексе | ~1.6 с (из них debounce 1.5 с — конфигурируется) |
| Корректное завершение (`daemon stop`) | БД на диске без `-wal`/`-shm` файлов |

### Технический долг

- Прогресс `0/0` в `daemon status` во время initial reindex (косметика — не обновляется из блокирующей фазы).
- Linux / macOS не проверены вживую — есть только теоретическая кроссплатформенность через crate `dirs` и `notify`. Просьба feedback при первых запусках на не-Windows.
- Интеграционных тестов для daemon_core нет — только unit-тесты на `config`, `ipc`, `state`.

## [0.4.0] — 2026-03-30

- `mtime`+`file_size` pre-filter для initial reindex: 93К файлов пере-проверяются за ~4 с вместо ~163 с (SHA-256 только для изменившихся файлов).
- Миграция `migrate_v3` — добавляет колонки `mtime`, `file_size` в таблицу `files`.
- Per-project PID-lock (`.code-index/serve.pid`) — защита от одновременного запуска двух `serve` на один проект.

## [0.3.0] — 2026-03-...

- Параллельный read+hash через rayon.
- `hash_ast` без `to_sexp` (быстрее).
- Снятие `max_file_size` для кода — большие BSL/XML-файлы теперь индексируются целиком.
- Тюнинг `mmap_size`, `batch_size` для initial indexation.
