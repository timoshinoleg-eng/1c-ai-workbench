# Optional client-owned external assistant settings.
# Do not put real keys into this repository or share them with the implementer.
# The 1C dump must remain local. If an MCP client sends snippets to an external
# service, that data transfer is controlled by the client's own account and settings.

EXTERNAL_CLIENT_PROVIDER=
EXTERNAL_CLIENT_PROFILE=
EXTERNAL_CLIENT_API_KEY=