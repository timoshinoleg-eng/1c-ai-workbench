# Runtime-only union of the workbench MCP services. Test tools remain in
# requirements.txt and are intentionally excluded from the production installer.
fastmcp>=3.0.0
httpx>=0.27.0
lxml>=5.2.0
pydantic>=2.7.0
